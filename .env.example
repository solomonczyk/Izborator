# Environment Configuration for Izborator
# Copy this file to .env and update the values for your environment

# ============================================
# DATABASE (PostgreSQL)
# ============================================
# REQUIRED: Must be set for Docker Compose
DB_USER=postgres
DB_PASSWORD=postgres_change_me_in_production
DB_NAME=izborator
DB_PORT=5432

# ============================================
# SEARCH ENGINE (Meilisearch)
# ============================================
# IMPORTANT: NEVER use 'masterKey123' in production!
# Generate a strong key: openssl rand -base64 32
MEILISEARCH_API_KEY=masterKey123

# ============================================
# CACHE (Redis)
# ============================================
# Optional: Redis doesn't require authentication by default
# If you want to use Redis password:
# REDIS_PASSWORD=redis_password_here

# ============================================
# TIME-SERIES DB (InfluxDB)
# ============================================
# REQUIRED: Must be set for Docker Compose
INFLUX_USER=admin
INFLUX_PASSWORD=influx_password_change_me
INFLUX_ORG=izborator_org
INFLUX_BUCKET=price_history

# ============================================
# BACKEND CONFIGURATION
# ============================================
# Log level: debug, info, warn, error
LOG_LEVEL=info

# Optional: Set to 'production' for production deployment
ENV=development

# OPTIONAL API Keys (required only if using those features)
# Get OpenAI key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Get Google API key from: https://cloud.google.com/docs/authentication/api-keys
GOOGLE_API_KEY=
GOOGLE_CX=

# ============================================
# FRONTEND CONFIGURATION
# ============================================
# Usually don't change this - points to backend in Docker network
NEXT_PUBLIC_API_BASE=http://backend:8080
