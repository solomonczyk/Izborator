# 🔧 Исправление проблемы с портом API

## Проблема

Фронтенд пытается подключиться к API на `http://localhost:8081`, но API запущен на `http://localhost:3002`.

## Решение

### Шаг 1: Остановите фронтенд (Ctrl+C в терминале)

### Шаг 2: Убедитесь, что переменная окружения установлена

Перед запуском фронтенда установите переменную окружения:

```powershell
$env:NEXT_PUBLIC_API_BASE="http://localhost:3002"
```

### Шаг 3: Очистите кэш Next.js (опционально, но рекомендуется)

```powershell
cd frontend
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
```

### Шаг 4: Перезапустите фронтенд

```powershell
$env:PORT="3003"
$env:NEXT_PUBLIC_API_BASE="http://localhost:3002"
npm run dev
```

Или используйте скрипт:
```powershell
.\start-dev.bat
```

## Альтернативное решение: Создайте .env.local

Создайте файл `frontend/.env.local`:

```
NEXT_PUBLIC_API_BASE=http://localhost:3002
PORT=3003
```

Затем просто запустите:
```powershell
npm run dev
```

## Проверка

После перезапуска:
1. Откройте `http://localhost:3003/sr/catalog`
2. Ошибка должна исчезнуть
3. Каталог должен загрузиться (даже если пустой)

## Если проблема сохраняется

1. Проверьте консоль браузера (F12) - там должен быть правильный URL запроса
2. Проверьте Network tab - посмотрите, на какой URL идут запросы
3. Убедитесь, что Backend API запущен на порту 3002

