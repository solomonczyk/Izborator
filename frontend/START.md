# Инструкция по запуску фронтенда

## ⚠️ Важно: порт 3000 занят другим проектом

Если вы видите 404 страницу от другого проекта (например, "Studio AI Solutions B&T"), значит на порту 3000 запущен другой сервер.

## ✅ Решение: используйте другой порт

### Вариант 1: Использовать скрипт (рекомендуется)

```powershell
cd frontend
.\start-dev.bat
```

Скрипт автоматически:
- Запустит фронтенд на порту **3003**
- Настроит `NEXT_PUBLIC_API_BASE=http://localhost:3002`

### Вариант 2: Ручной запуск

```powershell
cd frontend
$env:PORT="3003"
$env:NEXT_PUBLIC_API_BASE="http://localhost:3002"
npm run dev
```

### Вариант 3: Остановить другой проект (если не нужен)

```powershell
# Найти процесс на порту 3000
Get-NetTCPConnection -LocalPort 3000 | Select-Object -ExpandProperty OwningProcess

# Остановить процесс (замените PID)
Stop-Process -Id <PID> -Force

# Затем запустить на стандартном порту
cd frontend
npm run dev
```

## 🌐 Откройте в браузере:

После запуска откройте:
- **http://localhost:3003/sr/catalog** (если использовали порт 3003)
- **http://localhost:3000/sr/catalog** (если освободили порт 3000)

## ✅ Проверка:

Убедитесь, что:
- ✅ Backend API запущен на **http://localhost:3002**
- ✅ Next.js сервер запущен (порт 3003 или 3000)
- ✅ Переменная `NEXT_PUBLIC_API_BASE` указывает на правильный порт API

## 🔧 Настройка через .env.local (опционально)

Создайте файл `frontend/.env.local`:
```
NEXT_PUBLIC_API_BASE=http://localhost:3002
PORT=3003
```

## 🐛 Если все еще видите 404:

1. Проверьте, что вы открыли правильный URL: `http://localhost:3003/sr/catalog`
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Проверьте логи Next.js сервера в терминале
4. Убедитесь, что Backend API запущен и доступен

