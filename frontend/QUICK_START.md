# 🚀 Быстрый запуск фронтенда

## Шаг 1: Установка зависимостей (если еще не установлены)

```powershell
cd frontend
npm install
```

Это может занять несколько минут при первом запуске.

## Шаг 2: Запуск фронтенда

### Вариант A: Использовать скрипт (рекомендуется)

```powershell
.\start-dev.bat
```

### Вариант B: Ручной запуск

```powershell
$env:PORT="3003"
$env:NEXT_PUBLIC_API_BASE="http://localhost:3002"
npm run dev
```

## Шаг 3: Открыть в браузере

После запуска откройте:
- **http://localhost:3003/sr/catalog**

## Ожидаемый результат

В терминале вы увидите:
```
- ready started server on 0.0.0.0:3003
- Local:        http://localhost:3003
```

## Проверка

✅ Backend API должен быть запущен на `http://localhost:3002`  
✅ Frontend будет доступен на `http://localhost:3003`

## Troubleshooting

**Ошибка: "npm: command not found"**
- Убедитесь, что Node.js установлен: `node --version`
- Установите Node.js с https://nodejs.org/

**Ошибка: "Port 3003 is already in use"**
- Найдите свободный порт: `Get-NetTCPConnection -LocalPort 3003`
- Используйте другой порт: `$env:PORT="3004"`

**Ошибка: "Cannot find module"**
- Установите зависимости: `npm install`

