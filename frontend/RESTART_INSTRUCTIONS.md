# 🔄 Инструкция по перезапуску фронтенда

## Проблема: Фронтенд все еще использует порт 8081

Если фронтенд показывает ошибку с портом 8081, выполните следующие шаги:

## Шаг 1: Остановите фронтенд

В терминале, где запущен `npm run dev`, нажмите:
```
Ctrl + C
```

## Шаг 2: Очистите кэш Next.js

```powershell
cd frontend
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
```

## Шаг 3: Проверьте .env.local

Убедитесь, что файл `frontend/.env.local` существует и содержит:

```
NEXT_PUBLIC_API_BASE=http://localhost:3002
PORT=3003
```

Если файла нет, создайте его.

## Шаг 4: Перезапустите фронтенд

```powershell
npm run dev
```

Или используйте скрипт:
```powershell
.\start-dev.bat
```

## Проверка

После перезапуска:
1. Откройте `http://localhost:3003/sr/catalog`
2. Откройте DevTools (F12) → Network tab
3. Обновите страницу (F5)
4. Проверьте запросы - они должны идти на `http://localhost:3002`

## Если проблема сохраняется

1. Проверьте, что Backend API запущен:
   ```powershell
   Get-NetTCPConnection -LocalPort 3002
   ```

2. Проверьте health endpoint:
   ```powershell
   Invoke-WebRequest -Uri "http://localhost:3002/api/health" -Method GET
   ```

3. Проверьте переменные окружения в браузере:
   - Откройте Console в DevTools
   - Выполните: `console.log(process.env.NEXT_PUBLIC_API_BASE)`
   - Должно вывести: `http://localhost:3002`

