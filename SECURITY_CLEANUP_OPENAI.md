# 🔒 Удаление OpenAI API ключа из истории Git

## ⚠️ ВНИМАНИЕ

Этот процесс **перезапишет всю историю Git**! Убедись, что:
1. ✅ Все изменения закоммичены
2. ✅ Создана резервная копия репозитория
3. ✅ Все коллабораторы уведомлены

## 📋 Шаги для удаления ключа

### Шаг 1: Проверка наличия ключа

```bash
# Проверка в текущих файлах
git ls-files | grep "\.env"

# Проверка в истории
git log --all -p -S "sk-proj--cbh8PBPLPk"
```

### Шаг 2: Запуск скрипта очистки

**Вариант 1: Автоматический скрипт (Windows)**
```bash
remove-openai-key-from-history.bat
```

**Вариант 2: Ручная очистка**
```bash
# Удаление ключа из всех файлов истории
git filter-branch -f --tree-filter '
  find . -type f \( -name "*.env" -o -name "*.md" -o -name "*.go" -o -name "*.ts" -o -name "*.js" \) \
    ! -path "*/.git/*" ! -path "*/node_modules/*" | \
  while read f; do
    if [ -f "$f" ]; then
      sed -i.bak "s/sk-proj-[A-Za-z0-9_-]\{48,\}/REMOVED_OPENAI_API_KEY/g" "$f" 2>/dev/null
      sed -i.bak "s/OPENAI_API_KEY=sk-[A-Za-z0-9_-]\{48,\}/OPENAI_API_KEY=REMOVED/g" "$f" 2>/dev/null
      rm -f "$f.bak" 2>/dev/null
    fi
  done
' --prune-empty --tag-name-filter cat -- --all

# Удаление .env файлов из истории
git filter-branch -f --tree-filter 'find . -type f -name ".env" ! -path "*/.git/*" -delete 2>/dev/null || true' --prune-empty --tag-name-filter cat -- --all

# Очистка ссылок
git for-each-ref --format='delete %(refname)' refs/original | git update-ref --stdin
git reflog expire --expire=now --all
git gc --prune=now --aggressive
```

### Шаг 3: Проверка результата

```bash
# Проверка, что ключ удален
git log --all -p -S "sk-proj--cbh8PBPLPk"

# Если команда ничего не выводит - ключ удален
```

### Шаг 4: Принудительный push (ОПАСНО!)

```bash
# ⚠️ Это перезапишет удаленную историю!
git push --force --all
git push --force --tags
```

## 🔐 Дополнительные меры безопасности

1. **Пересоздать API ключ в OpenAI:**
   - Зайди в OpenAI Dashboard
   - Удали старый ключ
   - Создай новый ключ
   - Обнови `.env` файл на сервере

2. **Проверить использование ключа:**
   - Проверь логи OpenAI API на подозрительную активность
   - Установи лимиты использования для нового ключа

3. **Обновить ключ на сервере:**
   ```bash
   # На сервере
   cd ~/Izborator
   # Отредактируй .env файл с новым ключом
   nano .env
   ```

## ✅ Чеклист после очистки

- [ ] Ключ удален из истории Git
- [ ] История переписана и оптимизирована
- [ ] Изменения запушены с --force
- [ ] Старый ключ удален в OpenAI Dashboard
- [ ] Создан новый ключ в OpenAI
- [ ] Новый ключ обновлен на сервере
- [ ] Все коллабораторы уведомлены о force push

## 📝 Примечания

- После force push все коллабораторы должны выполнить:
  ```bash
  git fetch origin
  git reset --hard origin/main
  ```

- Если ключ был скомпрометирован, рекомендуется также:
  - Проверить логи доступа к API
  - Установить мониторинг использования
  - Рассмотреть возможность ротации ключей

