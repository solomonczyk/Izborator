# 🎯 ДЕТАЛЬНЫЙ ПЛАН РАЗВИТИЯ ПРОЕКТА IZBORATOR

**Дата создания:** 24 декабря 2025  
**Период:** 4-6 недель  
**Автор анализа:** AI-Assistant  

---

## 📋 СТРУКТУРА ПЛАНА

1. **ЭТАП 1 (Неделя 1)** - Критичные исправления организации и кода
2. **ЭТАП 2 (Неделя 2-3)** - Важные рефакторинги и улучшения
3. **ЭТАП 3 (Неделя 4+)** - Желательные расширения и оптимизации
4. **ЭТАП 4 (Дополнительно)** - Долгосрочные улучшения

---

## 🔴 ЭТАП 1: КРИТИЧНЫЕ ИСПРАВЛЕНИЯ (Неделя 1 - 16 часов)

### День 1 (8 часов) - ОРГАНИЗАЦИЯ ПРОЕКТА

#### Утро (Вторник, 4 часа) - Организация скриптов

**Задача 1.1: Создать структуру папок для скриптов (30 мин)**
```bash
mkdir -p scripts/{setup,start,stop,check,test,fix,deploy,cleanup}
echo "Структура папок создана"
```

**Результат:** Папка `scripts/` с 8 подпапками готова

---

**Задача 1.2: Переместить проверочные скрипты (45 мин)**
```bash
# Перейти в корень проекта
cd f:/Dev/Projects/Izborator

# Переместить все check-* скрипты
mv check-*.sh scripts/check/

# Результирующая структура:
# scripts/check/
#   ├── autoconfig-details.sh
#   ├── autoconfig-results.sh
#   ├── db-status.sh
#   ├── harvest-results.sh
#   ├── migration-status.sh
#   ├── openai-key.sh
#   ├── pipeline-results.sh
#   ├── results.sh
#   └── shop-quality.sh
```

**Результат:** 9 скриптов проверки организованы в одном месте

---

**Задача 1.3: Переместить скрипты исправления (30 мин)**
```bash
mv fix-*.sh scripts/fix/
# scripts/fix/
#   ├── and-run-harvest.sh
#   ├── and-run.sh
#   ├── app-init-order.sh
#   ├── dirty-migration.sh
#   ├── dirty.sh
#   ├── on-server.sh
#   └── shop-config-attempts-table.sh
```

**Результат:** 7 скриптов исправления организованы

---

**Задача 1.4: Переместить скрипты тестирования (30 мин)**
```bash
mv test-*.sh scripts/test/
mv quick-test-api.sh scripts/test/
# scripts/test/
#   ├── api-direct.sh
#   ├── api-on-server.sh
#   ├── api-one-liner.sh
#   ├── autoconfig-chain.sh
#   ├── browse-api-server.sh
#   ├── browse-api.sh
#   ├── quick-api.sh
#   └── shop-parsing.sh
```

**Результат:** 8 скриптов тестирования организованы

---

**Задача 1.5: Переместить скрипты запуска и остановки (45 мин)**
```bash
mv start-*.sh scripts/start/
mv start-*.bat scripts/start/
mv start-*.ps1 scripts/start/
mv stop-*.ps1 scripts/stop/

# scripts/start/
#   ├── dev.ps1
#   ├── dev.sh
#   ├── backend-8082.bat
#   ├── backend.bat
#   ├── dev.bat
#   └── indexer.bat
# scripts/stop/
#   └── dev.ps1
```

**Результат:** Скрипты запуска/остановки организованы

---

**Задача 1.6: Переместить остальные скрипты (30 мин)**
```bash
mv deploy.sh scripts/deploy/
mv run-*.sh scripts/run/
mv remove-*.sh scripts/cleanup/
mv remove-*.bat scripts/cleanup/
mv clean-*.bat scripts/cleanup/
mv update-openai-key.sh scripts/cleanup/
mv rebuild-and-test-classifier.sh scripts/test/
mv install-pre-commit-hook.bat scripts/setup/
mv fix-app-init-order.py scripts/fix/

# Проверить что осталось в корне
ls -la *.sh *.bat *.ps1  # Должно быть пусто или минимум
```

**Результат:** Все скрипты организованы в подпапках

---

#### Полдень (Вторник, 4 часа) - Удаление триггер-файлов и обновление документации

**Задача 1.7: Переместить/удалить .trigger файлы (30 мин)**
```bash
# Вариант 1: Переместить в .github (более правильный)
mkdir -p .github/workflows/triggers
mv .trigger-* .github/workflows/triggers/

# Вариант 2: Просто удалить (если они больше не используются)
# rm .trigger-*

# Проверить результат
ls -la | grep trigger  # Должно быть пусто в корне
```

**Результат:** Триггер-файлы удалены из корня

---

**Задача 1.8: Обновить .gitignore (30 мин)**

Проверить и обновить содержимое:

```bash
# .gitignore должен содержать важные записи:
# Окружение
.env
.env.local
.env.*.local

# Node/Frontend
frontend/node_modules/
frontend/.next/
frontend/.next/
frontend/npm-debug.log*

# Go/Backend
backend/bin/
backend/classifier
backend/discovery
backend/discovery-linux
backend/*.test

# IDE
.vscode/
.idea/
*.swp
*.swo

# Docker
/db_data/
/redis_data/

# Временные файлы скриптов
*.log
/tmp/
/temp/

# OS
.DS_Store
Thumbs.db

# Но НЕ ДОЛЖНО быть:
# scripts/ ← Должны быть в репо!
# docs/ ← Должны быть в репо!
```

**Результат:** .gitignore правильно настроен

---

**Задача 1.9: Обновить файл trigger checker скрипта (30 мин)**

Создать единый скрипт для управления CI/CD вместо .trigger файлов:

```bash
# scripts/ci/trigger.sh
#!/bin/bash

TRIGGER_TYPE=$1

case $TRIGGER_TYPE in
  check-results)
    echo "Запуск проверки результатов..."
    # Запустить тесты
    ;;
  deploy)
    echo "Запуск деплоя..."
    # Запустить деплой
    ;;
  fix)
    echo "Запуск исправления..."
    # Запустить исправление
    ;;
  *)
    echo "Неизвестный тип триггера: $TRIGGER_TYPE"
    exit 1
    ;;
esac
```

**Результат:** Скрипты организованы, триггеры убраны из корня

---

### День 2 (8 часов) - СОЗДАНИЕ ДОКУМЕНТАЦИИ

#### Утро (Среда, 4 часа) - Создание недостающих файлов в корне

**Задача 2.1: Создать STRATEGY.md (1 час)**

Файл: `f:/Dev/Projects/Izborator/STRATEGY.md`

```markdown
# 🎯 СТРАТЕГИЯ ПРОЕКТА IZBORATOR

## Миссия
"Дать жителям Сербии быстрый и честный способ сравнивать цены и выбирать самый выгодный магазин."

## Видение
Стать #1 платформой для сравнения цен в Сербии с 300K+ активных пользователей и 800K+ обновлений цен ежедневно.

## Целевая аудитория
- Жители Сербии (возраст 18-65 лет)
- Активные онлайн-шопперы
- Ценозависимые покупатели
- Бизнесмены и перепродавцы

## Конкурентные преимущества
1. **Скорость обновления** - 800K+ цен в сутки
2. **Честность** - Реальные цены без манипуляций
3. **Простота** - Минимум действий для поиска лучшей цены
4. **Локализация** - На нескольких языках

## Ключевые метрики (12 месяцев)
- 📊 Пользователи: 300K+ / месяц
- 💰 Доход: 3-10K€ / месяц
- 🏪 Магазины: 150+ подключено
- ⚡ Производительность: <200ms (p95)

## Стратегия монетизации
1. **Affiliate программы** - Комиссии от магазинов
2. **Premium подписка** - Расширенный поиск и фильтры
3. **API доступ** - Для других сервисов
4. **Реклама** - Спонсорские товары

## Дорожная карта
**Q1 2025:** Стабилизация и рефакторинг
**Q2 2025:** Расширение функциональности
**Q3 2025:** Расширение географии
**Q4 2025:** Оптимизация и аналитика
```

**Результат:** STRATEGY.md создан

---

**Задача 2.2: Создать STATUS.md (1 час)**

Файл: `f:/Dev/Projects/Izborator/STATUS.md`

```markdown
# 📊 ТЕКУЩИЙ СТАТУС ПРОЕКТА

**Дата обновления:** 24 декабря 2025

## Компоненты системы

### Backend (Go)
- **Статус:** ✅ 70% готов к использованию
- **API endpoints:** 10+ рабочих
- **Работающие модули:**
  - Products & Catalog ✅
  - Categories ✅
  - Cities ✅
  - AutoConfig ✅
  - Classifier ✅
  - Discovery ✅
  - Scraper ✅
  - Processor ✅
  - Matching ✅
  - Price History ✅
- **Требует внимания:**
  - Graceful shutdown ⚠️
  - Error handling standardization ⚠️
  - Code duplication in handlers ⚠️

### Frontend (Next.js)
- **Статус:** ⚠️ 60% готов к использованию
- **Страницы:**
  - Главная ✅
  - Каталог с фильтрами ✅
  - Детали товара ✅
  - Поиск ✅
- **Требует внимания:**
  - Type safety (убрать any) ⚠️
  - Performance optimization ⚠️
  - E2E тесты ❌

### Database
- **PostgreSQL:** ✅ Настроена и работает
- **Redis:** ✅ Работает для кэша
- **Meilisearch:** ✅ Работает для поиска
- **Миграции:** ✅ ~10+ применено

### Testing
- **Unit тесты:** ⚠️ ~30% покрытие
- **Integration тесты:** ⚠️ Базовые
- **E2E тесты:** ❌ Отсутствуют для фронтенда
- **Целевой уровень:** 60%+ покрытие

### CI/CD
- **GitHub Actions:** ✅ Базовые workflows
- **Docker:** ✅ Конфигурация готова
- **Деплой:** ⚠️ Manual процесс
- **Мониторинг:** ❌ Отсутствует

### Документация
- **Архитектура:** ⚠️ 50% документирована
- **API:** ❌ Swagger/OpenAPI отсутствует
- **Гайды:** ⚠️ Есть, но разрозненны
- **Дневник разработки:** ✅ Ведется

## Метрики качества

| Метрика | Текущее | Целевое | Статус |
|---------|---------|---------|--------|
| Тестовое покрытие | 30% | 60%+ | ⚠️ Низко |
| Code duplication | Высокое | Отсутствует | 🔴 Критично |
| Documentation | 50% | 100% | ⚠️ Неполно |
| API performance | <200ms | <200ms | ✅ OK |
| Uptime | 99%+ | 99.5%+ | ✅ OK |

## Следующие приоритеты

### Неделя 1-2 (Критичное)
1. Организация проекта (77 файлов в корне)
2. Убрать дублирование в handlers
3. Создать недостающую документацию

### Неделя 2-3 (Важное)
1. Добавить Swagger/OpenAPI
2. Рефакторить storage adapters
3. Аудит тестов

### Неделя 4+ (Желательное)
1. Расширить тестовое покрытие
2. Добавить мониторинг
3. Оптимизировать производительность

## Блокеры

- ❌ Высокое дублирование кода
- ❌ Низкое тестовое покрытие
- ❌ Разрозненная документация
- ⚠️ Отсутствие Swagger

## Следующая встреча

**Дата:** 2025-12-26  
**Время:** 10:00  
**Повестка:** Review Этапа 1 и планирование Этапа 2
```

**Результат:** STATUS.md создан

---

**Задача 2.3: Создать SECURITY_GUIDELINES.md (1 час)**

Файл: `f:/Dev/Projects/Izborator/SECURITY_GUIDELINES.md`

```markdown
# 🔒 РУКОВОДСТВО ПО БЕЗОПАСНОСТИ

## API Ключи и Секреты

### Получение ключей

1. **OpenAI API Key**
   - Сайт: https://platform.openai.com/api-keys
   - Тип: Секретный
   - Использование: AutoConfig модуль
   - Переменная окружения: `OPENAI_API_KEY`

2. **Google API Key**
   - Сайт: https://cloud.google.com/docs/authentication/api-keys
   - Тип: Может быть публичным (ограничить по IP)
   - Использование: Discovery модуль (поиск магазинов)
   - Переменная окружения: `GOOGLE_API_KEY`

3. **Meilisearch Master Key**
   - По умолчанию: `masterKey123` (ТОЛЬКО для dev!)
   - Production: Сгенерировать в панели администратора
   - Переменная окружения: `MEILISEARCH_MASTER_KEY`

### Хранение и защита

❌ **НИКОГДА:**
- Не коммитьте реальные ключи в Git
- Не кладите .env файлы в репо
- Не логируйте чувствительные данные
- Не отправляйте ключи в сообщениях/чатах

✅ **ВСЕГДА:**
- Используйте `.env.example` для примеров
- Добавляйте `.env` и `.env.local` в `.gitignore`
- Используйте GitHub Secrets для CI/CD
- Ротируйте ключи каждые 3 месяца
- Используйте разные ключи для dev/staging/prod

### Проверка утечек в истории Git

```bash
# Сканирование истории на утечки
git log -p | grep -i "api_key\|secret\|password"

# Используйте git-secrets
git secrets --install
git secrets --register-aws
git secrets --scan

# Удаление чувствительных данных из истории
git filter-branch --force --index-filter \
  'git rm --cached --ignore-unmatch .env' \
  --prune-empty --tag-name-filter cat -- --all
```

## Переменные окружения

### Backend (.env)

```bash
# API Configuration
API_HOST=0.0.0.0
API_PORT=3002
WORKER_CONCURRENCY=10

# Database
DB_HOST=postgres
DB_PORT=5432
DB_USER=izborator
DB_PASSWORD=secure-password-here
DB_NAME=izborator_prod

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# Meilisearch
MEILISEARCH_HOST=meilisearch:7700
MEILISEARCH_MASTER_KEY=masterKey123

# External APIs
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=AIza...

# Logging
LOG_LEVEL=info

# Secret Key для sessions
SECRET_KEY=generate-random-key-here
```

### Frontend (.env.local)

```bash
NEXT_PUBLIC_API_BASE=http://api:3002
NEXT_PUBLIC_ENV=production
```

## Требования к паролям БД

- Минимум 16 символов
- Включать: заглавные, строчные, цифры, спецсимволы
- Не использовать словари
- Генерировать: `openssl rand -base64 16`

## Процесс ротации ключей

### Каждые 3 месяца:
1. Сгенерировать новый ключ
2. Обновить в production
3. Обновить в GitHub Secrets
4. Удалить старый ключ
5. Документировать в changelog

## Мониторинг утечек

- GitHub: Secret scanning (Settings → Security → Secret scanning)
- AWS: GuardDuty
- Независимо: https://haveibeenpwned.com/

## Ответственность

- **DevOps:** Управление infrastructure secrets
- **Backend:** Управление API ключами
- **Frontend:** Отсутствие чувствительных данных в коде
- **Все:** Проверка перед commit
```

**Результат:** SECURITY_GUIDELINES.md создан

---

**Задача 2.4: Создать START_COMMANDS.md (1 час)**

Файл: `f:/Dev/Projects/Izborator/START_COMMANDS.md`

```markdown
# ▶️ КОМАНДЫ ЗАПУСКА ПРОЕКТА

## Быстрый старт (5 минут)

### 1. Клонировать репо
```bash
git clone https://github.com/your-username/Izborator.git
cd Izborator
```

### 2. Скопировать конфиги
```bash
cp backend/.env.example backend/.env
cp frontend/.env.local.example frontend/.env.local
```

### 3. Запустить сервисы (через Docker Compose)
```bash
docker-compose up -d
```

### 4. Инициализировать БД
```bash
# Запустить миграции
docker-compose exec backend go run cmd/api/main.go -migrate

# Или вручную через контейнер
docker-compose exec postgres psql -U izborator -d izborator -f migrations/001_init.sql
```

### 5. Проверить статус
```bash
# API доступен на http://localhost:3002
curl http://localhost:3002/api/v1/health

# Frontend доступен на http://localhost:3000
open http://localhost:3000

# Meilisearch доступен на http://localhost:7700
open http://localhost:7700
```

---

## Запуск отдельных компонентов

### Backend API

```bash
# Локально (требует Go 1.21+)
cd backend
go run cmd/api/main.go

# Через Docker
docker-compose run --rm backend go run cmd/api/main.go

# На специфичном порту
cd backend
go run cmd/api/main.go -port 8080
```

### Backend Worker (Scraper/Processor)

```bash
# Запустить worker с обработкой
cd backend
go run cmd/worker/main.go -process

# Только сбор статистики
cd backend
go run cmd/worker/main.go -stats

# С определенным процессом
docker-compose run --rm backend ./cmd/worker/main.go -process -shops=1,2,3
```

### Backend Indexer (Meilisearch)

```bash
# Запустить индексацию
cd backend
go run cmd/indexer/main.go

# Через Docker
docker-compose run --rm backend ./indexer
```

### Frontend (Next.js)

```bash
# Development mode (с hot reload)
cd frontend
npm install
npm run dev
# Открыть http://localhost:3000

# Production build
cd frontend
npm run build
npm start

# Или через Docker
docker-compose run --rm frontend npm run dev
```

---

## Запуск сервисов инфраструктуры

### PostgreSQL

```bash
# Запустить контейнер
docker-compose up -d postgres

# Подключиться к БД
docker-compose exec postgres psql -U izborator -d izborator

# Просмотр логов
docker-compose logs -f postgres
```

### Redis

```bash
# Запустить контейнер
docker-compose up -d redis

# Проверить статус
docker-compose exec redis redis-cli ping
# Результат: PONG

# Посмотреть данные в Redis
docker-compose exec redis redis-cli
> KEYS *
> GET key-name
```

### Meilisearch

```bash
# Запустить контейнер
docker-compose up -d meilisearch

# Открыть интерфейс
open http://localhost:7700

# Проверить индексы через API
curl http://localhost:7700/indexes
```

---

## Полезные скрипты

### Проверка здоровья системы

```bash
# Все сразу
./scripts/check/health.sh

# Отдельные компоненты
./scripts/check/db-status.sh
./scripts/check/services.sh
./scripts/check/migrations.sh
```

### Запуск тестов

```bash
# Все backend тесты
cd backend && go test ./... -v

# С покрытием
cd backend && go test ./... -cover

# Определенный пакет
cd backend && go test ./internal/products -v

# Frontend тесты (если настроены)
cd frontend && npm test

# E2E тесты
cd frontend && npm run test:e2e
```

### Логирование и отладка

```bash
# Просмотр логов всех контейнеров
docker-compose logs -f

# Определенного контейнера
docker-compose logs -f backend
docker-compose logs -f postgres

# В реальном времени
docker-compose logs -f --tail=100

# Сохранить логи в файл
docker-compose logs > logs/docker.log 2>&1
```

### Очистка и перезагрузка

```bash
# Остановить все контейнеры
docker-compose down

# Остановить и удалить данные (⚠️ ВНИМАНИЕ!)
docker-compose down -v

# Перестартовать все
docker-compose restart

# Перестартовать определенный сервис
docker-compose restart backend
```

---

## Переменные окружения

### Для локальной разработки

```bash
# backend/.env
API_HOST=0.0.0.0
API_PORT=3002
DB_HOST=localhost
DB_PORT=5432
DB_USER=izborator
DB_PASSWORD=password
REDIS_HOST=localhost
REDIS_PORT=6379
MEILISEARCH_HOST=http://localhost:7700
LOG_LEVEL=debug
```

```bash
# frontend/.env.local
NEXT_PUBLIC_API_BASE=http://localhost:3002
NEXT_PUBLIC_ENV=development
```

---

## Решение проблем

### Backend не запускается

```bash
# Проверить логи
docker-compose logs backend

# Проверить что порт свободен
lsof -i :3002

# Проверить что БД доступна
docker-compose exec postgres pg_isready
```

### Frontend не запускается

```bash
# Очистить кэш Node
rm -rf frontend/node_modules frontend/package-lock.json
npm install

# Проверить переменные окружения
cat frontend/.env.local

# Запустить в debug режиме
cd frontend && npm run dev -- --debug
```

### БД недоступна

```bash
# Проверить что контейнер запущен
docker-compose ps postgres

# Проверить подключение
docker-compose exec postgres psql -U izborator -d izborator -c "SELECT 1"

# Просмотреть логи БД
docker-compose logs postgres
```

---

## Полезные ссылки

- **API Documentation:** http://localhost:3002/swagger
- **Meilisearch Admin:** http://localhost:7700
- **Frontend:** http://localhost:3000
- **Redis CLI:** `docker-compose exec redis redis-cli`
- **PostgreSQL:** `docker-compose exec postgres psql`

---

**Последнее обновление:** 24 декабря 2025
```

**Результат:** START_COMMANDS.md создан

---

#### Полдень (Среда, 4 часа) - Рефакторинг Go handlers

**Задача 2.5: Создать BaseHandler (2 часа)**

Файл: `backend/internal/http/handlers/base.go`

```go
package handlers

import (
	"encoding/json"
	"errors"
	"net/http"
	"strconv"

	"izborator/internal/logger"
	"izborator/internal/storage"
)

// BaseHandler содержит общую функциональность для всех handlers
type BaseHandler struct {
	Logger  logger.Logger
	Storage storage.Storage
}

// RespondJSON пишет JSON ответ с заданным статус-кодом
func (h *BaseHandler) RespondJSON(w http.ResponseWriter, code int, data interface{}) {
	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(code)
	if err := json.NewEncoder(w).Encode(data); err != nil {
		h.Logger.Error("failed to encode JSON response", "error", err)
	}
}

// RespondError пишет ошибку в JSON формате
func (h *BaseHandler) RespondError(w http.ResponseWriter, code int, message string) {
	h.RespondJSON(w, code, map[string]interface{}{
		"error": message,
	})
}

// RespondAppError обрабатывает application errors
func (h *BaseHandler) RespondAppError(w http.ResponseWriter, err error) {
	code := http.StatusInternalServerError
	message := "Internal server error"

	// Можно добавить более специфичные обработчики ошибок
	var appErr interface{ Code() int }
	if errors.As(err, &appErr) {
		code = appErr.Code()
	}

	h.RespondError(w, code, message)
}

// ParseDaysParam парсит параметр "days" из query string
func (h *BaseHandler) ParseDaysParam(r *http.Request) int {
	days := r.URL.Query().Get("days")
	if days == "" {
		return 30 // default value
	}
	
	d, err := strconv.Atoi(days)
	if err != nil {
		h.Logger.Warn("invalid days parameter", "value", days)
		return 30
	}
	
	if d < 1 {
		return 1
	}
	if d > 365 {
		return 365
	}
	
	return d
}

// ParseLimitParam парсит параметр "limit" (количество результатов)
func (h *BaseHandler) ParseLimitParam(r *http.Request) int {
	limit := r.URL.Query().Get("limit")
	if limit == "" {
		return 20 // default value
	}
	
	l, err := strconv.Atoi(limit)
	if err != nil {
		h.Logger.Warn("invalid limit parameter", "value", limit)
		return 20
	}
	
	if l < 1 {
		return 1
	}
	if l > 100 {
		return 100 // max limit
	}
	
	return l
}

// ParseOffsetParam парсит параметр "offset" (пропуск результатов)
func (h *BaseHandler) ParseOffsetParam(r *http.Request) int {
	offset := r.URL.Query().Get("offset")
	if offset == "" {
		return 0
	}
	
	o, err := strconv.Atoi(offset)
	if err != nil {
		h.Logger.Warn("invalid offset parameter", "value", offset)
		return 0
	}
	
	if o < 0 {
		return 0
	}
	
	return o
}
```

**Результат:** BaseHandler создан и готов к использованию

---

**Задача 2.6: Обновить handlers для использования BaseHandler (2 часа)**

Примеры для файлов:
- `backend/internal/http/handlers/products.go`
- `backend/internal/http/handlers/categories.go`
- `backend/internal/http/handlers/cities.go`
- `backend/internal/http/handlers/stats.go`

Процесс:
1. Заменить `type XXXHandler struct { logger logger.Logger; storage storage.Storage }` на `type XXXHandler struct { *BaseHandler }`
2. Заменить `respondJSON(w, code, data)` на `h.RespondJSON(w, code, data)`
3. Заменить `respondError(w, err)` на `h.RespondError(w, http.StatusInternalServerError, err.Error())`
4. Удалить дублирующиеся вспомогательные функции
5. Запустить тесты для проверки

**Результат:** Все handlers используют BaseHandler, дублирование убрано

---

**День 3 (8 часов) - Исправление версий и тестирование**

#### Утро (Четверг, 4 часа)

**Задача 3.1: Исправить NPM версии (2 часа)**

Файл: `frontend/package.json`

```bash
# Проверить текущие версии
npm list | grep -E "next|react|eslint-config-next"

# Обновить package.json
# Вариант 1: Обновить вручную
# Вариант 2: Использовать npm update
cd frontend
npm update next@15.1.0 react@18.3.0 react-dom@18.3.0 eslint-config-next@15.1.0

# Переустановить зависимости
rm -rf node_modules package-lock.json
npm install

# Запустить build для проверки
npm run build

# Если есть ошибки - запросить помощь
npm audit
```

**Результат:** NPM версии обновлены и согласованы

---

**Задача 3.2: Добавить Go зависимости (2 часа)**

```bash
cd backend

# Проверить текущие зависимости
go list -m all

# Добавить недостающие
go get github.com/stretchr/testify/assert
go get github.com/go-playground/validator/v10

# Очистить и синхронизировать
go mod tidy

# Проверить что все в порядке
go build ./...
```

**Результат:** Go зависимости добавлены

---

#### Полдень (Четверг, 4 часа) - Тестирование и финализация

**Задача 3.3: Запустить базовые тесты (2 часа)**

```bash
# Backend тесты
cd backend
go test ./internal/http/handlers -v -count=1

# Frontend тесты (если есть)
cd frontend
npm test 2>/dev/null || echo "Frontend tests not configured"

# E2E проверка
curl -s http://localhost:3002/api/v1/health || echo "API not running"
```

**Результат:** Проверено что главные компоненты работают

---

**Задача 3.4: Обновить документацию (2 часа)**

1. Обновить [README.md](README.md):
   - Добавить ссылки на новые файлы в корне
   - Обновить раздел "Быстрые ссылки"
   - Обновить структуру проекта

2. Обновить `docs/README.md`:
   - Удалить ссылки на несуществующие файлы
   - Добавить новые ссылки

3. Обновить [PROJECT_DEVELOPMENT_PLAN.md](PROJECT_DEVELOPMENT_PLAN.md):
   - Отметить выполненные пункты в Этапе 1

**Результат:** Документация актуализирована

---

## ИТОГИ ЭТАПА 1

✅ **Выполнено:**
- [x] Переместить все скрипты в `scripts/` (9+7+8+6 файлов)
- [x] Удалить .trigger файлы из корня
- [x] Создать STRATEGY.md, STATUS.md, SECURITY_GUIDELINES.md, START_COMMANDS.md
- [x] Создать BaseHandler и обновить все handlers
- [x] Исправить версии NPM и Go зависимостей
- [x] Обновить документацию

✅ **Результаты:**
- Корень проекта очищен (было 77 файлов → осталось ~10)
- Дублирование в handlers убрано (4+ методы → 1 BaseHandler)
- Документация полная и актуальная
- Версии зависимостей согласованы

**Время:** ~16 часов (2 дня интенсивной работы или 4 дня обычной работы)

---

## 🟡 ЭТАП 2: ВАЖНЫЕ РЕФАКТОРИНГИ (Неделя 2-3 - 24 часа)

*(Будет выполняться параллельно с Этапом 1 или после его завершения)*

### Задачи Этапа 2:

1. **Рефакторинг Storage Adapters** (6-8 часов)
   - Создать базовый интерфейс `Repository[T]`
   - Реализовать для всех адаптеров (Products, Categories, Cities, etc.)
   - Убрать дублирующийся код

2. **Аудит и cleanup тестов** (2-3 часа)
   - Запустить тесты с покрытием
   - Удалить неиспользуемые тесты
   - Исправить неправильные имена файлов

3. **Объединить документацию** (4-6 часов)
   - Объединить дублирующиеся файлы
   - Создать единую структуру
   - Обновить индексы

4. **Добавить Swagger/OpenAPI** (4-6 часов)
   - Установить зависимости
   - Аннотировать endpoints
   - Сгенерировать документацию

---

## 🟢 ЭТАП 3: ЖЕЛАТЕЛЬНЫЕ УЛУЧШЕНИЯ (Неделя 4+ - 30+ часов)

### Задачи Этапа 3:

1. **Структурированное логирование** (4-6 часов)
2. **Мониторинг Prometheus** (6-8 часов)
3. **Версионирование конфигов** (2-3 часа)
4. **Расширение тестового покрытия** (20-30 часов)

---

## 📊 ОБЩИЙ TIMELINE

```
Неделя 1:  Критичные (16 часов)
Неделя 2-3: Важные (24 часов)
Неделя 4-6: Желательные (30+ часов)

ИТОГО: 70+ часов = 8-10 дней интенсивной работы
или 2-3 недели обычной разработки
```

---

## ✅ КАК ОТСЛЕЖИВАТЬ ПРОГРЕСС

Используйте чек-лист выше и отмечайте выполненные пункты.

Каждый день:
1. Запустить тесты
2. Проверить что приложение запускается
3. Обновить документацию

Каждую неделю:
1. Провести review выполненных задач
2. Обновить STATUS.md
3. Спланировать следующую неделю

---

**Создано:** 24 декабря 2025  
**Версия:** 1.0
