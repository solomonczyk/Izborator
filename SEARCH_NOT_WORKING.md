# 🔍 Почему не ищутся товары - Диагностика и Решение

**Дата:** 2025-12-30  
**Проблема:** Каталог на production сервере показывает "Ничего не найдено" при поиске

## 📊 Возможные причины

1. **❌ Товары не загружены в БД** - парсинг не произошел или товаров нет в БД
2. **❌ Товары не индексированы в Meilisearch** - индекс не создан или пуст
3. **⚠️ Проблема с подключением API** - frontend не может достучаться до backend

## 🔧 Решение

### Шаг 1: Проверить статус системы

```bash
# Запустить диагностику
./diagnose-search.sh
```

Проверит:
- ✅ Товары в PostgreSQL
- ✅ Индекс в Meilisearch
- ✅ API доступность
- ✅ Логи backend

### Шаг 2: Загрузить тестовые товары

Если товаров нет в БД, загружаем тестовые данные:

```bash
# Запустить скрипт загрузки
./load-test-products.sh
```

Что делает:
1. Загружает 4 тестовых товара (Nike, Samsung, Lenovo, TV)
2. Проверяет что товары в БД
3. Переиндексирует Meilisearch
4. Проверяет поиск работает

Тестовые товары должны быть найдены после этого.

### Шаг 3: Вручную переиндексировать (если нужно)

```bash
# SSH на сервер
ssh -i ~/.ssh/izborator_key root@152.53.227.37

# Перейти в директорию
cd /root/Izborator/backend

# Переиндексировать ВСЕ товары из БД в Meilisearch
go run cmd/indexer/main.go -reindex

# Или синхронизировать (добавить недостающие товары)
go run cmd/indexer/main.go -sync

# Или настроить индекс (поля для поиска)
go run cmd/indexer/main.go -setup
```

## 📌 Причины отсутствия товаров

### 1. Парсинг не работает
**Симптомы:** таблица `products` пуста, а `raw_products` пуста или много error'ов

**Решение:**
```bash
# Проверить логи worker
docker-compose logs --tail=50 backend | grep -i "worker\|scraper\|error"

# Проверить доступные магазины
docker-compose exec -T postgres psql -U postgres -d izborator -c \
  "SELECT id, name, is_active, catalog_url FROM shops LIMIT 10;"

# Запустить worker вручную
cd /root/Izborator/backend
go run cmd/worker/main.go -process
```

### 2. Индекс не создан в Meilisearch
**Симптомы:** товары есть в БД, но поиск не работает

**Решение:**
```bash
# Создать и заполнить индекс
cd /root/Izborator/backend
go run cmd/indexer/main.go -setup
go run cmd/indexer/main.go -reindex
```

### 3. Frontend не может подключиться к API
**Симптомы:** ошибка в console браузера, поиск не отправляет запрос

**Решение:**
```bash
# Проверить API здоров
curl http://152.53.227.37:8080/api/health

# Проверить CORS headers
curl -i http://152.53.227.37:8080/api/v1/products/search?q=test

# Проверить логи backend
docker-compose logs --tail=50 backend
```

## ✅ Проверка что всё работает

```bash
# 1. Товары в БД?
docker-compose exec -T postgres psql -U postgres -d izborator -c \
  "SELECT COUNT(*) FROM products;"

# 2. Товары в Meilisearch индексе?
curl http://meilisearch:7700/indexes/products/stats

# 3. Поиск работает?
curl "http://localhost:8080/api/v1/products/search?q=Samsung"

# 4. Frontend может подключиться?
curl "http://152.53.227.37/api/v1/products/search?q=test" -v
```

## 📝 Быстрая загрузка тестовых данных

Если нужны результаты сразу:

```bash
# На production сервере
ssh -i ~/.ssh/izborator_key root@152.53.227.37

cd /root/Izborator

# Загрузить тестовые товары
docker-compose exec -T postgres psql -U postgres -d izborator < \
  /root/Izborator/backend/scripts/seed_test_products.sql

# Переиндексировать
cd backend && go run cmd/indexer/main.go -reindex

# Готово! Тестируем
curl "http://localhost:8080/api/v1/products/search?q=Samsung"
```

После этого каталог должен показывать товары и поиск работать.

## 🚀 Следующие шаги

1. **Если тестовые товары работают** → переходим на реальные данные
   - Запустить паркинг: `go run cmd/worker/main.go -process`
   - Дождаться парсинга товаров
   - Переиндексировать

2. **Если проблема не решена** → проверить:
   - Логи каждого компонента (backend, indexer, database)
   - Сетевые соединения между контейнерами
   - Переменные окружения (.env файл)
   - Permissions в Meilisearch (API key правильный?)

---

**Last Updated:** 2025-12-30
