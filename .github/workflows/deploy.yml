name: Deploy to Production

on:
  push:
    branches:
      - main
  workflow_dispatch: # Позволяет запускать вручную

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
        
      - name: Verify SSH key is set
        if: env.SSH_PRIVATE_KEY == ''
        run: |
          echo "❌ Error: SSH_PRIVATE_KEY secret is not configured!"
          echo "Please add SSH_PRIVATE_KEY secret in GitHub Settings → Secrets and variables → Actions"
          exit 1
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add server to known hosts
        run: |
          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to server
        run: |
          ssh root@${{ secrets.SERVER_HOST }} << 'EOF'
            set -e
            cd ~/Izborator
            
            # Обновляем код (сбрасываем локальные изменения)
            echo "📥 Pulling latest changes..."
            git fetch origin
            git reset --hard origin/main
            git clean -fd
            
            # Генерируем SSL сертификаты, если их нет
            if [ ! -f nginx/ssl/cert.pem ]; then
              echo "🔐 Generating SSL certificates..."
              mkdir -p nginx/ssl
              cd nginx
              chmod +x generate-ssl.sh
              ./generate-ssl.sh
              cd ..
            fi
            
            # Останавливаем старые контейнеры (если есть)
            echo "🛑 Stopping old containers..."
            docker-compose down || true
            
            # Пересобираем и перезапускаем контейнеры
            echo "🔨 Building and restarting containers..."
            docker-compose build
            docker-compose up -d
            
            # Ждем запуска сервисов
            echo "⏳ Waiting for services to start..."
            sleep 10
            
            # Проверяем статус
            echo "✅ Checking services status..."
            docker-compose ps
            
            # Проверяем health
            echo "🏥 Health check..."
            curl -f http://localhost/api/health || exit 1
            
            echo "🎉 Deployment completed successfully!"
          EOF

      - name: Notify deployment status
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ Deployment successful!"
          else
            echo "❌ Deployment failed!"
          fi

