name: Check AutoConfig Details

on:
  workflow_dispatch: # Позволяет запускать вручную

jobs:
  check-details:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
        
      - name: Add server to known hosts
        run: |
          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Check AutoConfig Details
        run: |
          ssh -q -o LogLevel=ERROR root@${{ secrets.SERVER_HOST }} << 'EOF'
            set -e
            cd ~/Izborator
            
            # Обновляем код
            git pull || true
            
            # Делаем скрипт исполняемым
            chmod +x check-autoconfig-details.sh || true
            
            # Запускаем проверку
            bash check-autoconfig-details.sh
          EOF

      - name: Summary
        if: always()
        run: |
          echo "## 🔍 Детальная проверка AutoConfig" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Проверка выполнена. Смотри логи выше для деталей." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Что проверяется:" >> $GITHUB_STEP_SUMMARY
          echo "- Статистика по potential_shops" >> $GITHUB_STEP_SUMMARY
          echo "- Детали автоматически созданных магазинов" >> $GITHUB_STEP_SUMMARY
          echo "- Статистика по shop_config_attempts" >> $GITHUB_STEP_SUMMARY
          echo "- Качество конфигурации (селекторы)" >> $GITHUB_STEP_SUMMARY
          echo "- Оставшиеся классифицированные кандидаты" >> $GITHUB_STEP_SUMMARY

