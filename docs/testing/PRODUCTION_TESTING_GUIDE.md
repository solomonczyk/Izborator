# Руководство по тестированию AutoConfig на продакшене

> **Для тестирования на продакшен сервере см.:** [`TEST_ON_PRODUCTION_SERVER.md`](./TEST_ON_PRODUCTION_SERVER.md)

## Подключение к серверу

Сервер: `152.53.227.37` (v2202508292476370494.powersrv.de)

```bash
ssh root@152.53.227.37
cd ~/Izborator
```

## Текущее состояние

Сайт развернут на продакшене. Для полноценного тестирования AutoConfig с табличными данными нужны:

### 1. Классифицированные кандидаты
- **Требуется:** Кандидаты со `status = 'classified'`
- **Желательно:** С `site_type = 'service_provider'` для тестирования табличных данных
- **Минимум:** 5-10 кандидатов для статистически значимого теста

### 2. Реальные данные для тестирования

#### Типы сайтов для тестирования:

**E-commerce (уже работает):**
- ✅ Обычные интернет-магазины
- ✅ Товары с карточками
- ✅ Один товар на странице

**Service Provider (новое - нужно протестировать):**
- 🆕 Стоматологические клиники с прайс-листами
- 🆕 Парикмахерские салоны с ценами услуг
- 🆕 Ремонтные мастерские с таблицами цен
- 🆕 Образовательные курсы с ценами
- 🆕 Юридические услуги с прайс-листами
- 🆕 Транспортные услуги с ценами

## Проверка текущего состояния

### Вариант 1: Через скрипт (рекомендуется)

```bash
# Установить DATABASE_URL
export DATABASE_URL="postgresql://user:password@host:port/database"

# Запустить проверку
chmod +x scripts/production/check-autoconfig-data.sh
./scripts/production/check-autoconfig-data.sh
```

### Вариант 2: Через SQL напрямую

```sql
-- 1. Статистика по потенциальным магазинам
SELECT 
    status,
    COUNT(*) as count,
    COUNT(*) FILTER (WHERE metadata->>'site_type' = 'service_provider') as service_providers
FROM potential_shops
GROUP BY status;

-- 2. Классифицированные кандидаты
SELECT COUNT(*) 
FROM potential_shops 
WHERE status = 'classified';

-- 3. Service providers для тестирования таблиц
SELECT COUNT(*) 
FROM potential_shops 
WHERE status = 'classified' 
AND metadata->>'site_type' = 'service_provider';

-- 4. Примеры service_provider
SELECT 
    domain,
    confidence_score,
    metadata->>'site_type' as site_type,
    discovered_at
FROM potential_shops 
WHERE status = 'classified' 
AND metadata->>'site_type' = 'service_provider'
ORDER BY confidence_score DESC
LIMIT 10;
```

## Получение реальных данных

### Шаг 1: Discovery (поиск кандидатов)

Если классифицированных кандидатов мало или нет service_provider:

```bash
# Запустить Discovery для поиска сайтов услуг
# Discovery уже содержит запросы для услуг (стоматология, красота, ремонт и т.д.)
./backend/discovery -max-results 200

# Или через docker-compose
docker-compose run --rm backend ./discovery -max-results 200
```

**Что делает Discovery:**
- Ищет сайты через Google Search API
- Использует запросы типа `site:.rs "cenovnik usluga"`
- Находит сайты услуг с прайс-листами
- Сохраняет в `potential_shops` со статусом `new`

### Шаг 2: Classifier (классификация)

После Discovery нужно классифицировать найденные сайты:

```bash
# Классифицировать все новые кандидаты
./backend/classifier -classify-all -limit 100

# Или через docker-compose
docker-compose run --rm backend ./classifier -classify-all -limit 100
```

**Что делает Classifier:**
- Анализирует HTML страниц
- Определяет тип сайта (`ecommerce` или `service_provider`)
- Вычисляет confidence score
- Обновляет статус на `classified` для готовых кандидатов

### Шаг 3: AutoConfig (тестирование)

После классификации можно тестировать AutoConfig:

```bash
# Тестировать на 5 кандидатах
./backend/autoconfig -limit 5

# Или через docker-compose
docker-compose run --rm backend ./autoconfig -limit 5
```

**Что делает AutoConfig:**
- Берет классифицированных кандидатов
- Для `service_provider`: использует улучшенный промпт для таблиц
- Генерирует селекторы через OpenAI
- Валидирует селекторы (для таблиц проверяет множественные элементы)
- Создает магазины в таблице `shops`

## Что проверить в логах

### Для service_provider должны быть логи:

```
✅ Validation successful
  site_type: "service_provider"
  names_count: 5  (должно быть > 1 для таблиц)
  prices_count: 5
  first_name: "Стрижка мужская"
  first_price: "1500 RSD"
```

### Если найдена только одна услуга:

```
⚠️ Service provider found only one service, might not be a table
  names_count: 1
  prices_count: 1
```

Это не ошибка - может быть одна услуга на странице.

## Минимальные требования для тестирования

### Для базового теста:
- ✅ 3-5 классифицированных кандидатов
- ✅ Хотя бы 1-2 service_provider
- ✅ OPENAI_API_KEY настроен

### Для полноценного теста:
- ✅ 10-20 классифицированных кандидатов
- ✅ 5-10 service_provider
- ✅ Разнообразие типов услуг (стоматология, красота, ремонт)
- ✅ OPENAI_API_KEY с достаточным лимитом

## Автоматизация (Daemon режим)

Для непрерывной обработки:

```bash
# AutoConfig в режиме демона (обрабатывает кандидатов каждые 5 минут)
./backend/autoconfig -daemon -interval 5m -limit 3

# Или через docker-compose
docker-compose run -d backend ./autoconfig -daemon -interval 5m -limit 3
```

## Мониторинг результатов

### Проверка созданных магазинов:

```sql
-- Статистика по автоматически созданным магазинам
SELECT 
    COUNT(*) as total,
    COUNT(*) FILTER (WHERE selectors->>'name' IS NOT NULL) as with_name,
    COUNT(*) FILTER (WHERE selectors->>'price' IS NOT NULL) as with_price,
    COUNT(*) FILTER (
        WHERE selectors->>'name' IS NOT NULL 
        AND selectors->>'price' IS NOT NULL
    ) as with_both
FROM shops 
WHERE is_auto_configured = true;
```

### Проверка качества селекторов для таблиц:

```sql
-- Магазины с селекторами для таблиц
SELECT 
    name,
    base_url,
    selectors->>'name' as name_selector,
    selectors->>'price' as price_selector
FROM shops 
WHERE is_auto_configured = true
AND (
    selectors->>'name' LIKE '%table%' 
    OR selectors->>'name' LIKE '%tr%'
    OR selectors->>'name' LIKE '%td%'
)
ORDER BY created_at DESC;
```

## Troubleshooting

### Проблема: "no candidates available"
**Решение:** 
1. Проверить, есть ли кандидаты: `SELECT COUNT(*) FROM potential_shops WHERE status = 'classified';`
2. Если нет - запустить Discovery и Classifier

### Проблема: "AI generation failed"
**Решение:** 
1. Проверить OPENAI_API_KEY в .env
2. Проверить лимиты API (quota)
3. Проверить логи на детали ошибки

### Проблема: "validation failed" для service_provider
**Решение:**
1. Проверить логи - возможно, селекторы не находят данные
2. Проверить, что на странице действительно есть таблица
3. Возможно, нужна ручная настройка селекторов

### Проблема: Находится только одна услуга вместо таблицы
**Решение:**
- Это нормально, если на странице действительно одна услуга
- Проверить, что селекторы правильные (должны быть для таблиц, не для одного элемента)
- Возможно, нужно улучшить промпт для AI

## Следующие шаги

1. ✅ Проверить текущее состояние БД
2. ✅ Запустить Discovery для получения service_provider
3. ✅ Запустить Classifier для классификации
4. ✅ Запустить AutoConfig для тестирования
5. ✅ Проверить результаты и логи
6. ✅ При необходимости улучшить промпт или валидацию

