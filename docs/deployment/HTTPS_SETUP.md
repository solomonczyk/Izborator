# 🔒 Настройка HTTPS через nip.io и Let's Encrypt

## Шаг 1: Установка Certbot на сервере

Подключись к серверу и установи Certbot:

```bash
ssh root@152.53.227.37

# Обновляем пакеты
apt update

# Устанавливаем Certbot
apt install -y certbot
```

## Шаг 2: Остановка Docker Nginx

Нужно освободить порт 80 для проверки домена:

```bash
# Останавливаем Docker Nginx
docker stop izborator_nginx
```

## Шаг 3: Получение сертификата Let's Encrypt

```bash
# Генерируем сертификат для nip.io домена
certbot certonly --standalone -d 152.53.227.37.nip.io
```

**Что произойдет:**
1. Certbot попросит ввести email (для уведомлений о продлении)
2. Согласись с условиями (введи `Y`)
3. Если все ок, увидишь: `Congratulations! ... /etc/letsencrypt/live/152.53.227.37.nip.io/fullchain.pem`

## Шаг 4: Автоматическое обновление конфигурации

После получения сертификата, код уже обновлен:
- ✅ `docker-compose.yml` - монтирует `/etc/letsencrypt` в контейнер
- ✅ `nginx/nginx.conf` - использует Let's Encrypt сертификаты

Просто задеплой изменения:

```bash
# На локальной машине
git add .
git commit -m "feat: Enable HTTPS via nip.io with Let's Encrypt"
git push
```

CI/CD автоматически:
1. Остановит старые контейнеры
2. Пересоберет Nginx
3. Запустит с новыми сертификатами

## Шаг 5: Проверка

После успешного деплоя проверь:

```bash
# Должен открыться без предупреждения о самоподписанном сертификате
curl -I https://152.53.227.37.nip.io/api/health
```

В браузере:
- Открой `https://152.53.227.37.nip.io`
- Должен быть зеленый замочек 🔒 (валидный SSL сертификат)

## Автоматическое обновление сертификатов

Let's Encrypt сертификаты действительны 90 дней. Для автоматического обновления:

```bash
# На сервере создай cron job
crontab -e

# Добавь строку (обновление каждый месяц)
0 0 1 * * certbot renew --quiet && docker restart izborator_nginx
```

Или используй systemd timer (более надежно):

```bash
# Создай файл
nano /etc/systemd/system/certbot-renew.service
```

```ini
[Unit]
Description=Renew Let's Encrypt certificates
After=docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/certbot renew --quiet
ExecStartPost=/usr/bin/docker restart izborator_nginx
```

```bash
# Создай timer
nano /etc/systemd/system/certbot-renew.timer
```

```ini
[Unit]
Description=Run certbot renewal monthly

[Timer]
OnCalendar=monthly
Persistent=true

[Install]
WantedBy=timers.target
```

```bash
# Активируй timer
systemctl enable certbot-renew.timer
systemctl start certbot-renew.timer
```

## Если что-то пошло не так

**Ошибка: "Port 80 is already in use"**
```bash
# Проверь, что Docker Nginx остановлен
docker ps | grep nginx
docker stop izborator_nginx
```

**Ошибка: "Failed to obtain certificate"**
- Убедись, что порт 80 доступен извне
- Проверь firewall: `ufw status`
- Убедись, что домен 152.53.227.37.nip.io резолвится на твой IP

**Сертификат не обновляется в Docker**
```bash
# Перезапусти контейнер
docker restart izborator_nginx

# Или пересобери
cd ~/Izborator
docker-compose up -d --force-recreate nginx
```

## Использование своего домена

Если купишь домен, процесс тот же:

1. Настрой A-запись в DNS: `izborator.твойдомен.com → 152.53.227.37`
2. Получи сертификат: `certbot certonly --standalone -d izborator.твойдомен.com`
3. Обнови `nginx/nginx.conf`: замени `152.53.227.37.nip.io` на `izborator.твойдомен.com`
4. Закоммить и запушить

