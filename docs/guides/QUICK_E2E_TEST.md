# 🧪 Быстрый E2E тест

## ✅ Статус серверов

- **Backend API:** ✅ Работает на `http://localhost:3002`
- **Frontend:** ✅ Работает на `http://localhost:3003`
- **Товары:** ✅ Найдено 1 товар (Apple iPhone 15 Pro Max 256GB)

---

## 🌐 Открой в браузере

**URL:** http://localhost:3003/sr/catalog

---

## 📋 Чек-лист проверки

### 1. Проверка списков

**Категории:**
- Открой выпадающий список "Категорија"
- Если видишь реальные названия (не только "Све категорије") → ✅ **"Списки полные"**
- Если видишь только "Све категорије" → ⚠️ **"Списки пустые"** (нормально, если БД пустая)

**Города:**
- Открой выпадающий список "Град"
- Если видишь реальные названия (Beograd, Novi Sad и т.д.) → ✅ **"Списки полные"**
- Если видишь только "Сви градови" → ⚠️ **"Списки пустые"** (нормально, если БД пустая)

---

### 2. Проверка товаров

- Если видишь "Apple iPhone 15 Pro Max 256GB" → ✅ **"Товар виден"**
- Если видишь "Ништа није пронађено" → ⚠️ **"Товаров нет"**

---

### 3. Тест фильтрации по городу

**Действия:**
1. Выбери город из списка (если есть)
2. Нажми "Примени филтере"
3. Наблюдай результат

**Результат:**
- ✅ **"Товар исчез при фильтрации"** - фильтр работает (товар не подходит)
- ✅ **"Товар остался при фильтрации"** - фильтр работает (товар подходит)

---

### 4. Тест фильтрации по категории

**Действия:**
1. Выбери категорию из списка (если есть)
2. Нажми "Примени филтере"
3. Наблюдай результат

**Результат:**
- ✅ **"Товар исчез при фильтрации"** - фильтр работает
- ✅ **"Товар остался при фильтрации"** - фильтр работает

---

## 📊 Итоговый отчет

После проверки напиши:

```
✅ Списки: [полные/пустые]
✅ Товары: [видны/нет]
✅ Фильтр по городу: [товар исчез/остался]
✅ Фильтр по категории: [товар исчез/остался]
```

---

## 🔍 Проверка в DevTools

**Открой DevTools (F12) → Network:**
- Все запросы должны возвращать статус **200**
- Запросы должны идти на `http://localhost:3002` (не на 8081!)

**Открой DevTools (F12) → Console:**
- Не должно быть ошибок с красным фоном
- Предупреждение "Categories API returned non-array: null" должно отсутствовать

