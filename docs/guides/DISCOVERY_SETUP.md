# 🔍 Discovery Worker - Настройка и запуск

## 📝 Шаг 1: Получение ключей Google

### 1.1 Google Cloud API Key

1. Зайди в [Google Cloud Console](https://console.cloud.google.com/)
2. Создай новый проект (например, `izborator-discovery`)
3. Включи API: **"Custom Search API"**
   - Перейди в "APIs & Services" → "Library"
   - Найди "Custom Search API"
   - Нажми "Enable"
4. Создай API Key:
   - Перейди в "APIs & Services" → "Credentials"
   - Нажми "Create Credentials" → "API Key"
   - Скопируй ключ (он выглядит как `AIza...`)

### 1.2 Search Engine ID (CX)

1. Зайди в [Programmable Search Engine](https://programmablesearchengine.google.com/)
2. Нажми **"Add"**
3. Заполни форму:
   - **Name:** `Izborator Search`
   - **What to search:** Выбери **"Search the entire web"** (Искать во всем интернете)
   - **Region:** Выбери **"Serbia"** (это важно для релевантности)
   - **Image search:** Включи опцию (опционально)
4. Нажми **"Create"**
5. Скопируй **Search engine ID** (он выглядит как `cx=...`)

---

## 🚀 Шаг 2: Настройка переменных окружения

1. Скопируй `env.example` в `.env`:
```bash
cd backend
cp env.example .env
```

2. Открой `.env` и добавь свои ключи:
```env
GOOGLE_API_KEY=твой_google_api_key
GOOGLE_CX=твой_custom_search_engine_id
```

**⚠️ ВАЖНО:** Никогда не коммить `.env` файл в Git! Он уже в `.gitignore`.

## 🚀 Шаг 3: Запуск Discovery Worker

### Базовый запуск (использует .env):

```bash
cd backend
go run cmd/discovery/main.go
```

### С параметрами (опционально, переопределяют .env):

```bash
go run cmd/discovery/main.go \
  -max-results 50 \
  -delay 2s
```

### С переопределением ключей через флаги (для тестирования):

```bash
go run cmd/discovery/main.go \
  -key "твой_api_key" \
  -cx "твой_cx" \
  -max-results 50 \
  -delay 2s
```

**Параметры:**
- `-key` - Google API Key (опционально, если не задан в .env)
- `-cx` - Custom Search Engine ID (опционально, если не задан в .env)
- `-max-results` - Максимум результатов на запрос (по умолчанию: 100, максимум: 100)
- `-delay` - Задержка между запросами (по умолчанию: 1s)

---

## 📊 Что делает Discovery?

1. **Выполняет поисковые запросы** (dorking queries):
   - `site:.rs "dodaj u korpu"`
   - `site:.rs "kupi odmah"`
   - `site:.rs "cena rsd"`
   - И еще 7 запросов...

2. **Извлекает домены** из результатов Google

3. **Сохраняет в БД** (`potential_shops`):
   - Проверяет дубликаты (ON CONFLICT DO NOTHING)
   - Сохраняет метаданные (title, URL, query, page)

4. **Логирует результаты**:
   - `🆕 Discovered candidate: domain.com`
   - `✅ Discovery completed: discovered: X, skipped: Y`

---

## ⚠️ Важные ограничения Google API

- **100 результатов на запрос** (максимум)
- **100 запросов в день** (бесплатный тариф)
- **Не спамить!** Discovery автоматически добавляет задержки между запросами

---

## 🔄 Следующий шаг: Классификация

После того, как Discovery наполнит таблицу `potential_shops`, запусти классификатор:

```bash
# Классификация всех новых кандидатов
go run cmd/classifier/main.go -classify-all
```

(Этот функционал нужно будет добавить в следующий итерации)

---

## 📝 Проверка результатов

```sql
-- Посмотреть все обнаруженные домены
SELECT domain, source, status, discovered_at 
FROM potential_shops 
ORDER BY discovered_at DESC;

-- Статистика
SELECT 
  source,
  status,
  COUNT(*) as count
FROM potential_shops
GROUP BY source, status;
```

---

## 🐛 Troubleshooting

### Ошибка: "API Key and CX are required"
- Убедись, что в `.env` файле заданы `GOOGLE_API_KEY` и `GOOGLE_CX`
- Или передай параметры через флаги: `-key` и `-cx`

### Ошибка: "Google API returned error: 403"
- Проверь, что API Key правильный
- Убедись, что Custom Search API включен в Google Cloud Console

### Ошибка: "Google API returned error: 429"
- Превышен лимит запросов (100 в день)
- Подожди 24 часа или переключись на платный тариф

### Ошибка: "Failed to init app"
- Проверь, что `.env` файл настроен правильно
- Убедись, что PostgreSQL доступен

---

## ✅ Готово!

После успешного запуска Discovery:
1. Таблица `potential_shops` наполнится доменами
2. Каждый домен будет иметь статус `new`
3. Можно запускать классификатор для фильтрации

**Статус:** Discovery Worker готов к использованию! 🚀

