# 🚀 Project Horizon: От Вертикального Агрегатора к Горизонтальному Поисковику

## 📋 Видение

**Текущее состояние:** Вертикальный агрегатор (ручное добавление магазинов через SQL-скрипты)  
**Целевое состояние:** Горизонтальный поисковик (автоматическое обнаружение, классификация и настройка магазинов)

**Аналогия:** Переход от модели *E-katalog* к модели *Google Shopping* для территории Сербии (.rs)

---

## 🏗 Архитектура: Новые Модули

Добавляем три новых доменных модуля в `backend/internal/`:

1. **`discovery`** — Поиск новых URL (Google API, Bing, реестры)
2. **`classifier`** — Проверка "Магазин ли это?" (эвристика)
3. **`autoconfig`** — AI-генерация селекторов (LLM integration)

---

## 📅 Этап 1: Подготовка Базы Данных

### Миграция: `backend/migrations/XXXXXX_add_discovery_tables.sql`

```sql
-- Таблица для кандидатов (песочница)
CREATE TABLE potential_shops (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    domain VARCHAR(255) UNIQUE NOT NULL,        -- например, "tehnomanija.rs"
    source VARCHAR(50),                          -- "google_search", "manual", "registry"
    status VARCHAR(20) DEFAULT 'new',           -- new, classified, configured, rejected, active
    confidence_score FLOAT DEFAULT 0,            -- 0.0 - 1.0 (насколько уверены, что это магазин)
    discovered_at TIMESTAMPTZ DEFAULT NOW(),
    metadata JSONB,                              -- Заголовки, meta tags, контакты
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_potential_shops_status ON potential_shops(status);
CREATE INDEX idx_potential_shops_domain ON potential_shops(domain);

-- Расширение основной таблицы shops
ALTER TABLE shops ADD COLUMN IF NOT EXISTS is_auto_configured BOOLEAN DEFAULT FALSE;
ALTER TABLE shops ADD COLUMN IF NOT EXISTS ai_config_model VARCHAR(50); -- версия модели, которая создала конфиг
ALTER TABLE shops ADD COLUMN IF NOT EXISTS discovery_source VARCHAR(50); -- откуда найден магазин

-- Таблица для логов AI-конфигурации
CREATE TABLE shop_config_attempts (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    potential_shop_id UUID REFERENCES potential_shops(id),
    shop_id UUID REFERENCES shops(id),
    html_sample TEXT,                            -- Очищенный HTML для анализа
    ai_response JSONB,                           -- Ответ LLM
    validation_result JSONB,                     -- Результат проверки селекторов
    status VARCHAR(20),                          -- success, failed, pending
    error_message TEXT,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 📅 Этап 2: Модуль Discovery (Поисковик)

### Задача
Найти домены `.rs`, которые похожи на магазины.

### Реализация

**Структура модуля:**
```
backend/internal/discovery/
├── module.go          # Интерфейсы
├── impl.go            # Реализация
├── google.go          # Google Custom Search API
├── bing.go            # Bing Search API (fallback)
└── registry.go        # Парсинг реестров (yell.rs, planplus.rs)
```

**Интеграция с Google Custom Search JSON API:**
1. Регистрация API Key в Google Cloud Console
2. Настройка Programmable Search Engine на регион Сербия
3. Создание сервиса `DiscoveryService`

**Воркер «Разведчик» (`cmd/discovery/main.go`):**
- Работает по Cron (раз в неделю)
- Запросы:
  - `site:.rs "dodaj u korpu"` (добавить в корзину)
  - `site:.rs "kupi odmah"` (купить сейчас)
  - `site:.rs "cena RSD"`
  - `site:.rs inurl:proizvod`
- Парсит выдачу и сохраняет чистые домены в `potential_shops`
- Игнорирует домены, уже существующие в `shops`

**Технические детали:**
- Google API платный после 100 запросов/день
- Для MVP можно использовать парсинг через `SerpApi` или аккуратный скрапинг
- API надежнее, но дороже

---

## 📅 Этап 3: Модуль Classifier (Фильтр)

### Задача
Отсеять блоги, форумы и PDF-файлы. Определить, является ли сайт интернет-магазином.

### Реализация

**Структура модуля:**
```
backend/internal/classifier/
├── module.go          # Интерфейсы
├── impl.go            # Реализация
├── heuristics.go      # Эвристический анализ
└── patterns.go        # Паттерны для определения магазинов
```

**Воркер «Аудитор» (`cmd/classifier/main.go`):**
1. Берет домены со статусом `new` из `potential_shops`
2. Заходит на главную страницу (используя Colly или быстрый HTTP клиент)

**Эвристический анализ (Heuristics):**

**Проверка ключевых слов:**
- "Korpa", "Korpa za kupovinu", "Dodaj u korpu"
- "Cena", "Cijena", "RSD", "din"
- "Kupi", "Kupi odmah", "Naruči"
- "Proizvod", "Proizvodi", "Katalog"

**Проверка платформ:**
- Shopify: `shopify` в исходном коде
- WooCommerce: `woocommerce` в исходном коде
- Magento: `magento` в исходном коде
- OpenCart: `opencart` в исходном коде

**Проверка структуры:**
- Наличие иконки корзины (SVG/Class names: `cart`, `basket`, `korpa`)
- Наличие цен в формате `\d+ RSD` или `\d+\.\d+ RSD`
- Наличие кнопок действий (`Kupi`, `Dodaj`)

**Scoring система:**
```go
type ClassificationScore struct {
    KeywordsScore    float64 // 0.0 - 1.0
    PlatformScore   float64 // 0.0 - 1.0
    StructureScore  float64 // 0.0 - 1.0
    TotalScore      float64 // Weighted average
}
```

**Решение:**
- Если `TotalScore > 0.8` → статус `classified`
- Если `TotalScore > 0.5` → статус `pending_review` (требует ручной проверки)
- Иначе → `rejected`

---

## 📅 Этап 4: Модуль AutoConfig (AI Brain) 🧠

### Задача
Самая сложная. Превратить HTML в JSON-конфиг для Colly.

### Реализация

**Структура модуля:**
```
backend/internal/autoconfig/
├── module.go          # Интерфейсы
├── impl.go            # Реализация
├── html_cleaner.go    # Очистка HTML
├── llm_client.go      # Интеграция с LLM (OpenAI/Anthropic)
└── validator.go        # Валидация селекторов
```

**Алгоритм:**

1. **Поиск страницы товара:**
   - Воркер заходит на сайт
   - Ищет ссылку, похожую на товар:
     - `/proizvod/`, `/p/`, `/product/`
     - Глубокая вложенность (обычно товары глубже главной)
   - Выбирает первую найденную страницу товара

2. **Очистка HTML:**
   - Скачиваем HTML
   - Удаляем:
     - `<script>`, `<style>`, `<svg>`
     - Комментарии `<!-- -->`
     - Пустые теги
   - Оставляем только "скелет" тегов и классы
   - Обрезаем до ~15-20к токенов (чтобы влезло в контекст LLM дешево)

3. **LLM Integration (OpenAI / Anthropic):**
   - Отправляем очищенный HTML + Промпт
   - **Промпт:**
     ```
     Ты эксперт по CSS-селекторам. Вот HTML страницы товара интернет-магазина.
     
     Найди уникальные CSS-селекторы для:
     - Названия товара (name)
     - Цены (price)
     - Картинки товара (image)
     - Описания (description, опционально)
     - Бренда (brand, опционально)
     
     Верни строго JSON в формате:
     {
       "name": "h1.product-title",
       "price": ".current-price",
       "image": ".gallery img:first-of-type",
       "description": ".product-description",
       "brand": ".product-brand"
     }
     
     Важно: Селекторы должны быть уникальными и специфичными для этой страницы.
     ```

4. **Валидация:**
   - Получаем JSON от LLM
   - Тут же запускаем Colly с этими селекторами на **этой же** странице
   - Проверяем:
     - Цена > 0 и парсится как число
     - Имя не пустое
     - Картинка существует и доступна
   - Если данные извлеклись корректно → **УСПЕХ**
   - Сохраняем конфиг в `shops` (статус `active` или `pending_approval`)

**Обработка ошибок:**
- Если LLM вернул невалидный JSON → повторяем запрос (макс 3 попытки)
- Если селекторы не работают → помечаем как `failed`, сохраняем в `shop_config_attempts`
- Если требуется JS для рендеринга → используем `go-rod` для получения финального HTML

---

## 📅 Этап 5: UI для Администратора (Admin Dashboard)

### Задача
Ручной контроль и ревью автоматически найденных магазинов.

### Реализация (Frontend `/admin`)

**Страница "Найденные магазины":**
- Таблица из `potential_shops` со статусом `classified`
- Колонки:
  - Домен
  - Источник (google_search, manual, registry)
  - Confidence Score
  - Статус
  - Дата обнаружения
  - Действия

**Кнопка "Сгенерировать конфиг":**
- Запускает процесс AutoConfig вручную
- Показывает прогресс (loading state)
- После завершения показывает результат

**Ревьюер:**
- Показывает:
  - Ссылка на товар
  - Спаршенные данные (Картинка, Имя, Цена)
  - Предложенные селекторы
- Кнопки:
  - **"Approve"** — перенести в основные магазины (`shops`)
  - **"Edit Selectors"** — ручная правка селекторов
  - **"Reject"** — отклонить магазин

**Страница "AI Конфигурации":**
- История всех попыток AI-конфигурации
- Логи ошибок
- Статистика успешности

---

## 🛠 Технический Стек и Стоимость

### Язык и Фреймворки
- **Go** — существующие сервисы
- **Colly** — парсинг HTML (уже используется)
- **go-rod** — браузерная автоматизация (для JS-heavy сайтов)

### AI и API
- **OpenAI GPT-4o-mini** — самый дешевый и умный для таких задач
  - Стоимость анализа 1 магазина: < $0.01
  - Контекст: ~15-20к токенов на запрос
- **Anthropic Claude Haiku** — альтернатива (дешевле, но менее точный)
- **Google Custom Search API** — для Discovery
  - Платный после 100 запросов/день
  - ~$5 за 1000 запросов

### Прокси и Инфраструктура
- **Smartproxy** или **Bright Data** — для Discovery (чтобы Google не банил)
- **Rate Limiting** — встроенный в Colly (уже есть)

### База данных
- **PostgreSQL** — существующая БД
- Новые таблицы: `potential_shops`, `shop_config_attempts`

---

## 🚀 Roadmap (План Действий)

### Фаза 1: MVP Классификатор (2-3 недели)
**Цель:** Научиться определять, является ли сайт магазином.

**Задачи:**
1. ✅ Создать миграцию для `potential_shops`
2. ✅ Реализовать модуль `classifier`
3. ✅ Написать воркер `cmd/classifier/main.go`
4. ✅ Протестировать на 20 известных магазинах и 20 не-магазинах
5. ✅ Достичь точности > 85%

**Критерии успеха:**
- Классификатор корректно определяет магазины
- False positive rate < 15%

---

### Фаза 2: Discovery (2-3 недели)
**Цель:** Автоматически находить новые магазины.

**Задачи:**
1. ✅ Регистрация Google Custom Search API
2. ✅ Реализовать модуль `discovery`
3. ✅ Написать воркер `cmd/discovery/main.go`
4. ✅ Интеграция с `classifier` (автоматическая классификация найденных доменов)
5. ✅ Найти минимум 50 новых потенциальных магазинов

**Критерии успеха:**
- Discovery находит минимум 10 новых магазинов в неделю
- Классификатор корректно фильтрует результаты

---

### Фаза 3: AutoConfig MVP (3-4 недели)
**Цель:** Автоматически генерировать конфиги для простых магазинов.

**Задачи:**
1. ✅ Реализовать модуль `autoconfig`
2. ✅ Интеграция с OpenAI API
3. ✅ Реализовать HTML-очистку
4. ✅ Написать воркер `cmd/autoconfig/main.go`
5. ✅ Валидация селекторов
6. ✅ Протестировать на 10 магазинах

**Критерии успеха:**
- Успешная генерация конфига для 70% магазинов
- Валидация работает корректно

---

### Фаза 4: Admin Dashboard (2-3 недели)
**Цель:** UI для управления найденными магазинами.

**Задачи:**
1. ✅ Создать страницу `/admin/discovery`
2. ✅ Таблица найденных магазинов
3. ✅ Ревьюер конфигов
4. ✅ Интеграция с backend API

**Критерии успеха:**
- Админ может просматривать и одобрять магазины
- Удобный интерфейс для редактирования селекторов

---

### Фаза 5: Продвинутые Фичи (4-6 недель)
**Цель:** Улучшение точности и поддержка сложных случаев.

**Задачи:**
1. ✅ Поддержка JS-heavy сайтов (go-rod)
2. ✅ Улучшение промптов для LLM
3. ✅ Поддержка множественных страниц товаров (каталог)
4. ✅ Автоматическое определение `catalog_url`, `catalog_product_link`, `catalog_next_page`
5. ✅ Интеграция с реестрами (yell.rs, planplus.rs)

**Критерии успеха:**
- Поддержка 90%+ магазинов
- Автоматическая настройка каталогов

---

### Фаза 6: Масштабирование (ongoing)
**Цель:** Поддержка тысяч магазинов.

**Задачи:**
1. ✅ Оптимизация производительности
2. ✅ Кэширование результатов LLM
3. ✅ Пакетная обработка
4. ✅ Мониторинг и алерты

---

## 📊 Метрики Успеха

### Количественные
- **Discovery Rate:** Минимум 10 новых магазинов в неделю
- **Classification Accuracy:** > 85% точность
- **AutoConfig Success Rate:** > 70% успешных конфигов
- **False Positive Rate:** < 15%

### Качественные
- Автоматизация 90%+ процесса добавления магазинов
- Сокращение времени на добавление магазина с 30 минут до < 5 минут
- Поддержка всех основных платформ (Shopify, WooCommerce, Magento, OpenCart)

---

## 🎯 С чего начать (Первый Шаг)

**Задача на выходные (MVP Классификатор):**

Написать скрипт `cmd/classifier/main.go`, который:
1. Берет список из 10 известных сербских сайтов (не магазинов)
2. Берет список из 10 известных сербских магазинов
3. Скачивает их главные страницы
4. Программно определяет "Это магазин" или "Нет", считая слова "Korpa" и "RSD"
5. Выводит метрики точности

**Если классификатор заработает — полдела сделано. Остальное — дело техники.**

---

## 📝 Примечания

### Ограничения
- Google API имеет лимиты (100 запросов/день бесплатно)
- LLM API платные (но очень дешевые для этой задачи)
- Некоторые сайты требуют JS для рендеринга (нужен go-rod)

### Будущие Улучшения
- Поддержка Instagram/Facebook магазинов (сложнее, требует браузерную автоматизацию)
- Парсинг блогов с рекомендациями товаров (требует NLP)
- Автоматическое обновление конфигов при изменении структуры сайта

---

## 🔗 Связанные Документы

- `CATALOG_PARSER_SETUP.md` — Настройка автоматического парсинга каталогов
- `DEPLOY.md` — Инструкции по деплою
- `DEVELOPMENT_LOG.md` — Лог разработки

---

**Дата создания:** 2025-01-16  
**Версия:** 1.0  
**Статус:** 🟢 В разработке

