# 🧪 Руководство по E2E тестированию

## Подготовка

### 1. Запуск сервисов

```bash
# Запуск Docker контейнеров
docker-compose up -d

# Проверка статуса
docker-compose ps
```

### 2. Запуск Backend API

```bash
cd backend

# Убедитесь, что .env файл настроен
# MEILISEARCH_API_KEY=masterKey123

# Запуск API сервера (порт 3002)
$env:SERVER_PORT="3002"  # PowerShell
go run cmd/api/main.go

# Или используйте скрипт:
.\start-backend.bat
```

Ожидаемый результат:
```
{"level":"info","message":"Successfully connected to PostgreSQL"}
{"level":"info","message":"Meilisearch connection established"}
{"level":"info","message":"Redis connection established"}
{"level":"info","port":3002,"message":"Starting API server"}
```

**Примечание:** API сервер запущен на порту **3002** (избегает конфликтов с другими сервисами).

**Если порт занят:**
- Ошибка: `bind: 10048` (Windows) или `address already in use` (Linux)
- Решение: Остановить процесс на порту или изменить `SERVER_PORT` в `.env`

### 3. Запуск Frontend

**⚠️ Важно:** Если порт 3000 занят другим проектом, используйте другой порт.

```bash
cd frontend

# Установка зависимостей (если нужно)
npm install

# Вариант 1: Использовать скрипт (порт 3003)
.\start-dev.bat

# Вариант 2: Ручной запуск на порту 3003
$env:PORT="3003"
$env:NEXT_PUBLIC_API_BASE="http://localhost:3002"
npm run dev
```

Ожидаемый результат:
```
- ready started server on 0.0.0.0:3003
- Local:        http://localhost:3003
```

**Примечание:** Frontend будет доступен на порту **3003** (или 3000, если он свободен).

## Тестирование фильтрации

### Тест 1: Загрузка категорий

1. Откройте http://localhost:3003/sr/catalog (или http://localhost:3000/sr/catalog, если использовали стандартный порт)
2. Проверьте выпадающий список "Категорија"
3. **Ожидаемый результат:**
   - Список категорий загружается из API
   - Категории отображаются с отступами (подкатегории)
   - Есть опция "Све категорије"

**Проверка API напрямую:**
```bash
# PowerShell
Invoke-WebRequest -Uri "http://localhost:3002/api/health" -Method GET

# Проверка категорий
Invoke-WebRequest -Uri "http://localhost:3002/api/v1/categories/tree?lang=sr" -Method GET
```

**Примечание:** Если порт 8080 занят, можно использовать другой порт через переменную окружения `SERVER_PORT`.

### Тест 2: Загрузка городов

1. На странице каталога проверьте выпадающий список "Град"
2. **Ожидаемый результат:**
   - Список городов загружается из API
   - Города отсортированы по `sort_order`
   - Есть опция "Сви градови"

**Проверка API напрямую:**
```bash
Invoke-WebRequest -Uri "http://localhost:3002/api/v1/cities?lang=sr" -Method GET
```

### Тест 3: Фильтрация по категории

1. Выберите категорию из списка (например, "Мобилни телефони")
2. Нажмите "Примени филтере"
3. **Ожидаемый результат:**
   - URL содержит параметр `?category=mobilni-telefoni`
   - Отображаются только товары из выбранной категории
   - В логах API видно использование `category_id`

**Проверка API:**
```bash
Invoke-WebRequest -Uri "http://localhost:3002/api/v1/products/browse?category=mobilni-telefoni&page=1&per_page=20" -Method GET
```

### Тест 4: Фильтрация по городу

1. Выберите город из списка (например, "Београд")
2. Нажмите "Примени филтере"
3. **Ожидаемый результат:**
   - URL содержит параметр `?city=beograd`
   - Отображаются только товары с ценами в выбранном городе
   - Товары без цен в городе исключены

**Проверка API:**
```bash
Invoke-WebRequest -Uri "http://localhost:3002/api/v1/products/browse?city=beograd&page=1&per_page=20" -Method GET
```

### Тест 5: Комбинированная фильтрация

1. Выберите категорию И город
2. Добавьте фильтр по цене (например, от 50000 до 200000)
3. Нажмите "Примени филтере"
4. **Ожидаемый результат:**
   - URL содержит все параметры: `?category=...&city=...&min_price=...&max_price=...`
   - Отображаются товары, соответствующие ВСЕМ фильтрам
   - Цены в указанном диапазоне
   - Только в выбранном городе
   - Только из выбранной категории

**Проверка API:**
```bash
Invoke-WebRequest -Uri "http://localhost:3002/api/v1/products/browse?category=mobilni-telefoni&city=beograd&min_price=50000&max_price=200000" -Method GET
```

## Проверка данных

### Проверка в базе данных

```sql
-- Проверка категорий
SELECT id, slug, name_sr, level FROM categories WHERE is_active = true ORDER BY sort_order;

-- Проверка городов
SELECT id, slug, name_sr FROM cities WHERE is_active = true ORDER BY sort_order;

-- Проверка товаров с категориями
SELECT p.id, p.name, c.slug as category_slug 
FROM products p 
LEFT JOIN categories c ON p.category_id::text = c.id 
LIMIT 10;

-- Проверка цен по городу
SELECT pp.product_id, pp.shop_id, pp.price, c.name_sr as city_name
FROM product_prices pp
LEFT JOIN cities c ON pp.city_id::text = c.id
WHERE c.slug = 'beograd'
LIMIT 10;
```

## Ожидаемое поведение

### Успешный сценарий

1. ✅ Категории загружаются из API
2. ✅ Города загружаются из API
3. ✅ Фильтрация по категории работает
4. ✅ Фильтрация по городу работает
5. ✅ Комбинированная фильтрация работает
6. ✅ Цены отображаются корректно
7. ✅ Пагинация работает с фильтрами

### Обработка ошибок

1. ✅ Если API недоступен - показывается предупреждение
2. ✅ Если категории не загрузились - показывается предупреждение
3. ✅ Если города не загрузились - показывается предупреждение
4. ✅ Форма работает даже при ошибках загрузки

## Отладка

### Проблемы с загрузкой категорий/городов

1. Проверьте, что API сервер запущен
2. Проверьте CORS настройки (если фронтенд на другом порту)
3. Проверьте логи API сервера
4. Проверьте Network tab в DevTools браузера

### Проблемы с фильтрацией

1. Проверьте, что в БД есть данные:
   - Категории в таблице `categories`
   - Города в таблице `cities`
   - Товары с `category_id`
   - Цены с `city_id`

2. Проверьте логи API при запросе `/api/v1/products/browse`

3. Проверьте, что Meilisearch работает (если используется):
```bash
curl http://localhost:7700/health
```

## Чек-лист тестирования

- [ ] Категории загружаются из API
- [ ] Города загружаются из API
- [ ] Фильтрация по категории работает
- [ ] Фильтрация по городу работает
- [ ] Комбинированная фильтрация работает
- [ ] Цены отображаются корректно
- [ ] Пагинация сохраняет фильтры
- [ ] Ошибки обрабатываются корректно
- [ ] UI отзывчивый и понятный

