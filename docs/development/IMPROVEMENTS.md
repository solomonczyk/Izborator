# 🚀 Список улучшений системы Izborator

## 🔴 Критические (высокий приоритет)

### 1. **Отсутствие тестов**
**Проблема:** Нет ни одного теста в проекте  
**Решение:**
- Добавить unit-тесты для всех сервисов (processor, matching, scraper)
- Добавить integration-тесты для storage адаптеров
- Добавить E2E тесты для API endpoints
- Минимальное покрытие: 70% для критичных модулей

**Файлы для тестирования:**
- `backend/internal/processor/impl.go` - логика обработки товаров
- `backend/internal/matching/impl.go` - алгоритм матчинга
- `backend/internal/storage/*_adapter.go` - все адаптеры
- `backend/internal/http/handlers/*.go` - HTTP handlers

---

### 2. **Graceful Shutdown воркеров**
**Проблема:** TODO в `backend/cmd/worker/main.go:117`  
**Решение:**
```go
// Ожидаем завершения всех активных задач
var wg sync.WaitGroup
// Отменяем контексты для всех воркеров
// Ждём завершения с таймаутом
```

---

### 3. **Хардкод настроек пула БД**
**Проблема:** В `backend/internal/storage/postgres.go:30-33` настройки пула захардкожены, не используются из конфига  
**Решение:**
```go
config.MaxConns = cfg.MaxConnections  // из config.DBConfig
config.MinConns = cfg.MinConnections // добавить в конфиг
config.MaxConnLifetime = cfg.ConnMaxLifetime
config.MaxConnIdleTime = cfg.MaxIdleTime
```

---

### 4. **Использование context.Background() вместо передачи контекста**
**Проблема:** Множество мест используют `context.Background()` вместо передачи контекста из запроса  
**Места:**
- `backend/internal/storage/postgres.go:70`
- `backend/internal/storage/scraper_adapter.go:23` (использует `pg.Context()` - глобальный)
- `backend/internal/storage/pricehistory_adapter.go:22`

**Решение:** Передавать `context.Context` из HTTP handlers через все слои

---

### 5. **Потенциальная паника в normalizeBrand**
**Проблема:** `backend/internal/processor/impl.go:193` - может паниковать на пустой строке  
**Решение:**
```go
func (s *Service) normalizeBrand(brand string) string {
    brand = strings.TrimSpace(brand)
    if brand == "" {
        return ""
    }
    return strings.ToUpper(brand[:1]) + strings.ToLower(brand[1:])
}
```

---

## 🟡 Важные (средний приоритет)

### 6. **Отсутствие валидации входных данных**
**Проблема:** Нет валидации в handlers перед обработкой  
**Решение:**
- Добавить валидацию UUID для product_id
- Валидация URL для scraper
- Валидация цен (не отрицательные, разумные лимиты)
- Валидация пагинации (limit, offset)

**Библиотека:** Использовать `github.com/go-playground/validator` или встроенную валидацию

---

### 7. **Улучшение нормализации в matching**
**Проблема:** TODO в `backend/internal/matching/impl.go:57`  
**Решение:**
- Удаление спецсимволов
- Замена сокращений (GB → Gigabyte)
- Удаление артиклей (a, an, the)
- Нормализация чисел (256GB = 256 GB)

---

### 8. **Отсутствие retry механизма для внешних сервисов**
**Проблема:** Нет retry для Meilisearch, Redis при временных сбоях  
**Решение:**
- Добавить exponential backoff для Meilisearch
- Retry для Redis операций
- Circuit breaker pattern для защиты от каскадных сбоев

---

### 9. **Нет ограничений на размер батча**
**Проблема:** `ProcessRawProducts` принимает любой batchSize  
**Решение:**
```go
if batchSize <= 0 {
    batchSize = 10
}
if batchSize > 100 {
    batchSize = 100 // защита от перегрузки
}
```

---

### 10. **Отсутствие метрик и мониторинга**
**Проблема:** Нет метрик для отслеживания производительности  
**Решение:**
- Добавить Prometheus метрики:
  - Количество обработанных товаров
  - Время обработки батча
  - Количество ошибок
  - Размер очереди raw_products
- Health check endpoint (уже есть, но можно расширить)

---

### 11. **Улучшение обработки ошибок**
**Проблема:** Некоторые ошибки теряют контекст  
**Решение:**
- Использовать `fmt.Errorf("context: %w", err)` везде
- Добавить trace_id в логи для отслеживания запросов
- Структурированные ошибки с кодами

---

### 12. **Отсутствие rate limiting**
**Проблема:** Нет защиты от DDoS на API endpoints  
**Решение:**
- Добавить middleware для rate limiting (используя Redis)
- Лимиты по IP: 100 req/min для поиска, 10 req/min для остального

---

## 🟢 Желательные (низкий приоритет)

### 13. **Кэширование на уровне приложения**
**Проблема:** Нет кэширования часто запрашиваемых данных  
**Решение:**
- Кэшировать категории (редко меняются)
- Кэшировать популярные товары
- TTL: 5 минут для товаров, 1 час для категорий

---

### 14. **Улучшение логирования**
**Проблема:** Не все важные события логируются  
**Решение:**
- Добавить structured logging с trace_id
- Логировать время выполнения операций
- Логировать размеры батчей

---

### 15. **Оптимизация SQL запросов**
**Проблема:** Некоторые запросы можно оптимизировать  
**Решение:**
- Добавить индексы для часто используемых полей
- Использовать EXPLAIN ANALYZE для анализа
- Оптимизировать запросы в `browseViaPostgres` (сейчас получает 1000 товаров для фильтрации)

---

### 16. **Валидация URL в scraper**
**Проблема:** Нет проверки валидности URL перед парсингом  
**Решение:**
```go
func validateURL(url string) error {
    u, err := url.Parse(url)
    if err != nil {
        return err
    }
    if u.Scheme != "http" && u.Scheme != "https" {
        return errors.New("invalid scheme")
    }
    return nil
}
```

---

### 17. **Улучшение обработки price_history**
**Проблема:** TODO в `backend/internal/storage/pricehistory_adapter.go:175`  
**Решение:**
- Реализовать очистку старых данных (старше 1 года)
- Архивация в отдельную таблицу
- Cron job для периодической очистки

---

### 18. **Документация API**
**Проблема:** Нет OpenAPI/Swagger документации  
**Решение:**
- Добавить Swagger/OpenAPI спецификацию
- Использовать `github.com/swaggo/swag` для генерации

---

### 19. **Улучшение конфигурации**
**Проблема:** `getEnvAsSlice` не парсит правильно (TODO в `config.go:158`)  
**Решение:**
```go
func getEnvAsSlice(key string, defaultValue []string) []string {
    valueStr := os.Getenv(key)
    if valueStr == "" {
        return defaultValue
    }
    return strings.Split(valueStr, ",")
}
```

---

### 20. **Добавить миграции для индексов**
**Проблема:** Индексы могут отсутствовать в production  
**Решение:**
- Создать миграцию для добавления индексов:
  - `products.name` (для поиска)
  - `products.brand` (для фильтрации)
  - `raw_products.processed` (для выборки необработанных)
  - `product_prices.product_id, shop_id` (для быстрого поиска цен)

---

## 📊 Приоритизация

### Неделя 1 (Критично):
1. ✅ Тесты для processor и matching
2. ✅ Graceful shutdown воркеров
3. ✅ Исправление паники в normalizeBrand
4. ✅ Использование конфига для пула БД

### Неделя 2 (Важно):
5. ✅ Валидация входных данных
6. ✅ Retry механизм для внешних сервисов
7. ✅ Ограничения на batch size
8. ✅ Улучшение обработки ошибок

### Неделя 3 (Желательно):
9. ✅ Метрики и мониторинг
10. ✅ Rate limiting
11. ✅ Кэширование
12. ✅ Оптимизация SQL

---

## 🎯 Метрики успеха

- **Покрытие тестами:** > 70%
- **Время обработки батча:** < 5 секунд для 10 товаров
- **Uptime:** > 99.9%
- **Ошибки:** < 0.1% от всех запросов
- **Response time API:** < 200ms для 95% запросов

---

## 📝 Заметки

- Все улучшения должны быть задокументированы
- Каждое улучшение должно быть протестировано
- Приоритет: безопасность > производительность > удобство

