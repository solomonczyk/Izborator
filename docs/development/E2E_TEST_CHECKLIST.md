# ✅ E2E Тестирование - Чек-лист

## 🚀 Шаг 1: Запуск серверов

### Backend API
```powershell
cd F:\Dev\Projects\Izborator\backend
$env:SERVER_PORT="3002"
go run cmd/api/main.go
```

**Ожидаемый результат:**
```
{"level":"info","port":3002,"message":"Starting API server"}
```

### Frontend
```powershell
cd F:\Dev\Projects\Izborator\frontend
npm run dev
```

**Ожидаемый результат:**
```
- Local:        http://localhost:3003
```

---

## 🌐 Шаг 2: Открыть в браузере

Открой: **http://localhost:3003/sr/catalog**

---

## ✅ Чек-лист проверки

### 1. Проверка загрузки списков

**Категории:**
- [ ] Открой выпадающий список "Категорија"
- [ ] Проверь, что там есть опции (не только "Све категорије")
- [ ] Если категорий нет - это нормально (БД может быть пустой)
- [ ] Если категории есть - проверь, что они отображаются с отступами (подкатегории)

**Города:**
- [ ] Открой выпадающий список "Град"
- [ ] Проверь, что там есть опции (не только "Сви градови")
- [ ] Если городов нет - это нормально (БД может быть пустой)
- [ ] Если города есть - проверь названия (Beograd, Novi Sad и т.д.)

**Результат:**
- ✅ **"Списки полные"** - если видишь реальные названия
- ⚠️ **"Списки пустые"** - если только "Све категорије" и "Сви градови" (нормально, если БД пустая)

---

### 2. Проверка отображения товаров

- [ ] Проверь, что товары отображаются в списке
- [ ] Если видишь "Apple iPhone 15 Pro Max 256GB" - это тестовый товар
- [ ] Проверь, что отображается цена (175 000 RSD)
- [ ] Проверь, что отображается количество магазинов

**Результат:**
- ✅ **"Товар виден"** - если видишь хотя бы один товар
- ⚠️ **"Товаров нет"** - если видишь "Ништа није пронађено" (нормально, если БД пустая)

---

### 3. Тест фильтрации по городу

**Действия:**
1. Выбери город из списка (например, "Beograd" или любой другой)
2. Нажми кнопку "Примени филтере"
3. Наблюдай результат

**Ожидаемые результаты:**

**Вариант A: Товар исчез**
- ✅ Это **нормально** и подтверждает, что фильтр работает!
- Причина: у товара может не быть привязки к выбранному городу
- Это означает, что фильтрация работает корректно

**Вариант B: Товар остался**
- ✅ Это тоже нормально
- Причина: товар имеет привязку к выбранному городу
- Это означает, что фильтрация работает и показывает правильные результаты

**Результат:**
- ✅ **"Товар исчез при фильтрации"** - фильтр работает, товар не подходит под критерии
- ✅ **"Товар остался при фильтрации"** - фильтр работает, товар подходит под критерии

---

### 4. Тест фильтрации по категории

**Действия:**
1. Выбери категорию из списка
2. Нажми кнопку "Примени филтере"
3. Наблюдай результат

**Ожидаемые результаты:**
- Аналогично фильтрации по городу
- Товар может исчезнуть или остаться в зависимости от привязки к категории

---

### 5. Проверка консоли браузера

**Открой DevTools (F12) → Console:**
- [ ] Нет ошибок с красным фоном
- [ ] Нет предупреждений "Categories API returned non-array: null"
- [ ] Ошибки `content.js` можно игнорировать (это расширения браузера)

**Открой DevTools (F12) → Network:**
- [ ] Запросы к `/api/v1/categories/tree` возвращают статус 200
- [ ] Запросы к `/api/v1/cities` возвращают статус 200
- [ ] Запросы к `/api/v1/products/browse` возвращают статус 200
- [ ] Все запросы идут на `http://localhost:3002` (не на 8081!)

---

## 📊 Итоговый отчет

После проверки заполни:

```
✅ Списки: [полные/пустые]
✅ Товары: [видны/нет]
✅ Фильтр по городу: [товар исчез/остался]
✅ Фильтр по категории: [товар исчез/остался]
✅ Консоль: [без ошибок/есть ошибки]
```

---

## 🔧 Если что-то не работает

### Проблема: Списки пустые
**Решение:** Это нормально, если в БД нет категорий/городов. Можно добавить тестовые данные.

### Проблема: Товары не отображаются
**Решение:** 
1. Проверь, что в БД есть товары: `SELECT COUNT(*) FROM products;`
2. Если товаров нет, запусти парсинг: `go run cmd/worker/main.go -process`

### Проблема: Фильтр не работает
**Решение:**
1. Проверь Network tab - запросы должны идти на правильный URL
2. Проверь, что параметры передаются в URL после нажатия "Примени филтере"

---

## 🎯 Критерии успеха

✅ Все API endpoints отвечают статусом 200  
✅ Frontend загружается без ошибок  
✅ Списки категорий и городов отображаются (даже если пустые)  
✅ Товары отображаются (если есть в БД)  
✅ Фильтры работают (товар исчезает или остается в зависимости от критериев)

