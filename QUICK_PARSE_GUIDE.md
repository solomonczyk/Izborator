# 🚀 Быстрый гайд: Парсинг товаров по категориям

## ✅ Что уже готово

- ✅ Селекторы настроены для магазина `shop-001`
- ✅ Парсер готов к работе
- ✅ Воркер поддерживает флаги `-url` и `-process`

---

## 📋 Шаг 1: Найди валидные URL

### Для мобильных телефонов (Mobilni telefoni)

1. Открой https://gigatron.rs
2. Перейди в раздел **"Мобилни телефони"** или используй поиск **"iPhone"**
3. Выбери любой товар (например, iPhone 15)
4. Скопируй URL из адресной строки
5. **Пример:** `https://gigatron.rs/mobilni-telefoni/apple-iphone-15-128gb-black-mtp03zda-573380`

### Для ноутбуков (Laptopovi)

1. Открой https://gigatron.rs
2. Перейди в раздел **"Лаптопови"** или используй поиск **"laptop"**
3. Выбери любой товар (например, Lenovo или HP)
4. Скопируй URL из адресной строки
5. **Пример:** `https://gigatron.rs/laptopovi/lenovo-ideapad-3-15-82h7000vra`

---

## 🔧 Шаг 2: Запусти парсинг

### Для мобильного телефона:

```powershell
cd backend
go run cmd/worker/main.go -url "ТВОЙ_URL_ДЛЯ_ТЕЛЕФОНА" -shop "shop-001"
```

**Ожидаемый результат:**
```
✅ SUCCESS! Product parsed & saved
name: Apple iPhone 15 128GB Black
price: 159999
currency: RSD
```

### Для ноутбука:

```powershell
cd backend
go run cmd/worker/main.go -url "ТВОЙ_URL_ДЛЯ_НОУТБУКА" -shop "shop-001"
```

---

## 🔄 Шаг 3: Обработай сырые данные

После парсинга всех товаров, обработай их:

```powershell
cd backend
go run cmd/worker/main.go -process
```

**Ожидаемый результат:**
```
✅ Processing completed processed=2
```

---

## 📊 Шаг 4: Проверь результаты

### В БД:

```sql
SELECT name, price, brand, category FROM products ORDER BY created_at DESC LIMIT 5;
```

### На фронтенде:

1. Открой http://localhost:3003/sr/catalog
2. Товары должны появиться в каталоге
3. Фильтры по категориям должны работать

---

## 🎯 Итоговый чек-лист

- [ ] Найден URL для мобильного телефона
- [ ] Запущен парсинг телефона (успешно)
- [ ] Найден URL для ноутбука
- [ ] Запущен парсинг ноутбука (успешно)
- [ ] Запущена обработка сырых данных
- [ ] Товары появились на фронтенде
- [ ] Фильтры по категориям работают

---

## 💡 Полезные команды

**Проверка сырых данных:**
```sql
SELECT name, price, url FROM raw_products WHERE processed = false ORDER BY created_at DESC;
```

**Проверка обработанных товаров:**
```sql
SELECT name, price, brand FROM products ORDER BY created_at DESC LIMIT 5;
```

**Очистка тестовых данных (если нужно):**
```sql
DELETE FROM products WHERE name LIKE 'Test%';
DELETE FROM raw_products WHERE name LIKE 'Test%';
```

---

## 📝 Документация

- Подробная инструкция: `backend/scripts/PARSE_BY_CATEGORY.md`
- Интерактивный скрипт: `backend/scripts/find_and_test_urls.ps1`
- Настройка селекторов: `backend/scripts/update_shop_selectors.sql`

