# 🤖 Настройка GitHub Actions CI/CD

## Шаг 1: Генерация SSH ключа

На локальной машине (если еще нет):

```bash
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions
```

## Шаг 2: Копирование публичного ключа на сервер

```bash
ssh-copy-id -i ~/.ssh/github_actions.pub root@152.53.227.37
```

Или вручную:
```bash
cat ~/.ssh/github_actions.pub | ssh root@152.53.227.37 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

## Шаг 3: Добавление Secrets в GitHub

1. Перейди в репозиторий на GitHub
2. Settings → Secrets and variables → Actions
3. Нажми "New repository secret"

Добавь два секрета:

### `SSH_PRIVATE_KEY`
```bash
# Скопируй содержимое приватного ключа
cat ~/.ssh/github_actions
```
Вставь весь вывод (включая `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`)

### `SERVER_HOST`
```
152.53.227.37
```

## Шаг 4: Проверка workflow

1. Перейди в Actions на GitHub
2. Должен появиться workflow "Deploy to Production"
3. Можно запустить вручную: Actions → Deploy to Production → Run workflow

## Шаг 5: Тестирование автоматического деплоя

```bash
# Сделай небольшое изменение в коде
echo "# Test" >> README.md
git add README.md
git commit -m "test: CI/CD deployment"
git push
```

После push workflow должен автоматически запуститься и задеплоить изменения на сервер.

## 🔍 Проверка логов

В GitHub:
- Actions → Deploy to Production → выбери последний run → смотри логи

На сервере:
```bash
# Проверь, что изменения применились
cd ~/Izborator
git log -1

# Проверь статус контейнеров
docker-compose ps
```

## ⚠️ Troubleshooting

**Ошибка: "Permission denied (publickey)"**
- Проверь, что `SSH_PRIVATE_KEY` добавлен правильно (включая BEGIN/END строки)
- Проверь, что публичный ключ добавлен в `~/.ssh/authorized_keys` на сервере

**Ошибка: "Host key verification failed"**
- Workflow автоматически добавляет сервер в known_hosts
- Если проблема остается, проверь `SERVER_HOST` в Secrets

**Ошибка: "Connection refused"**
- Проверь, что сервер доступен: `ping 152.53.227.37`
- Проверь firewall на сервере

## 🎯 Результат

После настройки:
- ✅ При каждом `git push` в `main` автоматически деплоится на сервер
- ✅ Не нужно вручную подключаться к серверу для обновления
- ✅ Все изменения применяются автоматически

