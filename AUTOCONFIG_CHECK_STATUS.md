# ✅ Автоматическая проверка AutoConfig настроена

## 🎯 Что сделано

1. **Создан GitHub Actions workflow** `.github/workflows/check-autoconfig.yml`
   - Автоматический запуск каждый день в 6:00 UTC
   - Возможность ручного запуска через GitHub Actions UI

2. **Workflow проверяет:**
   - ✅ Автоматически созданные магазины (`is_auto_configured = true`)
   - ✅ Статистику по магазинам (auto_configured vs manual)
   - ✅ Статус `shop_config_attempts` (success/failed)
   - ✅ Статус `potential_shops` (new/classified)
   - ✅ Конфигурацию `.env` файла (наличие обязательных переменных)
   - ✅ Статус контейнеров (backend, worker)

3. **Исправлена конфигурация docker-compose.yml:**
   - ✅ `OPENAI_API_KEY=${OPENAI_API_KEY}` (без дефолта, обязательный)
   - ✅ Применено для `backend` и `worker` сервисов

## 🚀 Как запустить проверку

### Вариант 1: Через GitHub Actions UI (рекомендуется)

1. Перейди на GitHub: https://github.com/solomonczyk/Izborator/actions
2. Выбери workflow "Check AutoConfig Results"
3. Нажми "Run workflow" → "Run workflow"

### Вариант 2: Автоматически

Workflow запустится автоматически:
- Каждый день в 6:00 UTC
- После каждого деплоя (если настроить trigger)

### Вариант 3: Вручную на сервере

```bash
# Выполни на сервере
chmod +x check-autoconfig-results.sh
./check-autoconfig-results.sh
```

## 📊 Что будет проверено

1. **Магазины:**
   - Количество автоматически созданных магазинов
   - Список магазинов с `is_auto_configured = true`
   - Статистика (auto vs manual)

2. **Конфигурация:**
   - Наличие `.env` файла
   - Настройка `OPENAI_API_KEY`
   - Настройка `DB_PASSWORD`
   - Настройка `MEILISEARCH_API_KEY`

3. **Статус системы:**
   - Статус контейнеров backend и worker
   - Статус potential_shops (кандидаты для обработки)

## ✅ Ожидаемые результаты

После успешного AutoConfig:

- **В таблице `shops`:**
  - Записи с `is_auto_configured = true`
  - `is_active = true`
  - `base_url` и `name` заполнены

- **В таблице `shop_config_attempts`:**
  - Записи со статусом `success`

- **В логах workflow:**
  - Детальная статистика
  - Информация о конфигурации

## 🔧 Если нужно исправить .env на сервере

```bash
# На сервере
cd ~/Izborator

# Проверить текущий .env
cat .env | grep OPENAI_API_KEY

# Если отсутствует или пустой - создать из примера
cp .env.example .env

# Отредактировать и добавить реальные значения
nano .env

# Пересоздать контейнеры
docker-compose up -d --force-recreate backend worker
```

## 📝 Следующие шаги

1. ✅ Workflow создан и запушен
2. ⏳ Запустить workflow через GitHub Actions UI
3. ⏳ Проверить результаты в логах workflow
4. ⏳ При необходимости исправить `.env` на сервере

