# 🔒 Очистка секретов из репозитория

## ✅ Что уже сделано

1. **Все ключи вынесены в переменные окружения:**
   - Google API Key → `GOOGLE_API_KEY`
   - Google CX ID → `GOOGLE_CX`
   - Все ключи теперь читаются из `.env` файла

2. **Обновлен код:**
   - `backend/internal/config/config.go` - добавлена структура `GoogleConfig`
   - `backend/cmd/discovery/main.go` - теперь читает ключи из `.env`
   - `backend/env.example` - добавлены шаблоны для Google API

3. **Очищена документация:**
   - Удален CX ID из `DEVELOPMENT_LOG.md`
   - Обновлен `DISCOVERY_SETUP.md` с инструкциями по `.env`

## 🚀 Следующие шаги

### 1. Создай `.env` файл

```bash
cd backend
cp env.example .env
```

Затем открой `.env` и добавь свои реальные ключи:
```env
GOOGLE_API_KEY=твой_реальный_google_api_key
GOOGLE_CX=твой_реальный_cx_id
```

### 2. Очистка истории Git

⚠️ **ВНИМАНИЕ:** Это перезапишет всю историю Git!

1. **Создай резервную копию:**
   ```bash
   git clone --mirror . ../Izborator-backup
   ```

2. **Запусти скрипт очистки:**
   ```bash
   clean-secrets-history.bat
   ```

3. **Проверь результат:**
   ```bash
   git log --all --oneline
   git grep "AIzaSy"  # Не должно быть результатов
   git grep "REMOVED_CX_ID"  # Не должно быть результатов
   ```

4. **Принудительно запушь изменения:**
   ```bash
   git push --force --all
   git push --force --tags
   ```

⚠️ **ВАЖНО:** 
- Уведоми всех коллабораторов перед `--force` push
- Они должны будут сделать `git fetch --all` и `git reset --hard origin/main`

### 3. Ротация ключей (рекомендуется)

После очистки истории **обязательно**:
1. Пересоздай Google API Key в Google Cloud Console
2. Обнови `GOOGLE_API_KEY` в `.env`
3. Старый ключ больше не будет работать

## 📋 Проверка

После всех действий убедись:
- ✅ `.env` файл существует и содержит ключи
- ✅ `.env` в `.gitignore` (уже есть)
- ✅ В истории Git нет ключей (`git grep "AIzaSy"` возвращает пусто)
- ✅ Код работает с ключами из `.env`

## 🔐 Безопасность на будущее

- ✅ **Никогда** не коммить `.env` файл
- ✅ Используй `env.example` как шаблон
- ✅ Все ключи только в переменных окружения
- ✅ Регулярно проверяй репозиторий на утечки (GitGuardian, GitHub Secret Scanning)

