# Модульная архитектура (DDD Light)

## 🧱 Принцип слоёв (Hex-style)

```
cmd/api, cmd/worker            — entrypoints
        ↓
internal/http (API Layer)      — транспортный слой (HTTP)
        ↓
internal/* (Domain Layer)      — бизнес-логика + интерфейсы (contracts)
        ↓
internal/storage (Adapters)    — реализации доменных интерфейсов
        ↓
Drivers (Postgres, Redis, Meilisearch)
```

## Принципы

### 1. Модули = бизнес-области

Не технические слои, а отдельные смыслы.

### 2. Зависимости только сверху вниз (по import)

По зависимостям кода:

HTTP → Domain ← Storage → Drivers

- HTTP знает о Domain (сервисы)
- Domain знает только о себе и стандартной библиотеке
- Storage знает о Domain (интерфейсы, модели) и о драйверах

### 3. Каждый модуль самодостаточен

Никаких скрытых побочных эффектов.

### 4. Storage не содержит бизнес-логики

- Storage реализует доменные интерфейсы (repositories, adapters)
- Внутри — только маппинг доменных моделей в SQL/JSON/HTTP-запросы
- Никаких правил предметной области, только доступ к данным

### 5. HTTP — тонкая оболочка

Никакой логики внутри хендлеров.

## Жёсткие правила зависимостей (import)

### API слой (internal/http) может импортировать:

- доменные модули (`products`, `scraper`, `matching`, `pricehistory`, `processor`)
- `logger`, `config`, общие утилиты

**HTTP НЕ импортирует `storage` и драйверы.** Все wiring делается в `cmd/*` или `internal/app`.

### Domain слой (products, scraper, matching, pricehistory, processor) может импортировать:

- только стандартную библиотеку
- общие доменные типы (при необходимости — через чёткие интерфейсы, чтобы не создавать клубок зависимостей)

### Storage слой может импортировать:

- доменные модули (для реализаций интерфейсов)
- только стандартную библиотеку
- драйверы (pgx, redis, meilisearch)

### Запрещено:

- ❌ HTTP → storage
- ❌ HTTP → drivers
- ❌ Domain → http
- ❌ Domain ↔ Domain (кросс-зависимости без явных интерфейсов)

Взаимодействие между слоями только через интерфейсы домена.

## Interface-first подход

Каждый доменный модуль обязан содержать:

- `service.go`    — интерфейс сервиса
- `impl.go`       — реализация
- `models.go`     — сущности
- `errors.go`     — ошибки

## Пример правильной зависимости

HTTP вызывает ProductsService:

```go
p := products.New(storage.ProductsRepo)
p.GetByID(id)
```

Но не лезет в Postgres напрямую.

---

После сохранения этого файла у тебя появится официальная спецификация проекта — это дисциплинирует код и избавит от "архитектурного долга".
