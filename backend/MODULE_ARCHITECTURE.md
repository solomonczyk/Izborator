# Модульная архитектура (DDD Light)

## 🧱 Принцип слоёв

```
cmd/api, cmd/worker
        ↓
internal/http (API Layer)
        ↓
internal/products, internal/scraper, internal/matching, internal/pricehistory (Domain Layer)
        ↓
internal/storage (Storage Layer)
        ↓
Внешние ресурсы (Postgres, Redis, Meilisearch)
```

## Принципы

### 1. Модули = бизнес-области

Не технические слои, а отдельные смыслы.

### 2. Зависимости только сверху вниз

HTTP → Domain → Storage

Но НИКОГДА наоборот.

### 3. Каждый модуль самодостаточен

Никаких скрытых побочных эффектов.

### 4. Storage не знает о бизнес-логике

Только CRUD и драйверы.

### 5. HTTP — тонкая оболочка

Никакой логики внутри хендлеров.

## Жёсткие правила зависимостей

### API слой (internal/http) может импортировать:

- products
- scraper
- matching
- pricehistory
- storage (через сервисы, а не напрямую)

### Domain слой (products, scraper, matching…) может импортировать:

- storage

### Storage слой может импортировать:

- только стандартную библиотеку
- драйверы (pgx, redis, meilisearch`)

### Запрещено:

- ❌ HTTP → storage напрямую
- ❌ Domain → http
- ❌ Storage → domain
- ❌ Domain ↔ Domain (кросс-зависимости)

Взаимодействие только через интерфейсы.

## Interface-first подход

Каждый доменный модуль обязан содержать:

- `service.go`    — интерфейс сервиса
- `impl.go`       — реализация
- `models.go`     — сущности
- `errors.go`     — ошибки

## Пример правильной зависимости

HTTP вызывает ProductsService:

```go
p := products.New(storage.ProductsRepo)
p.GetByID(id)
```

Но не лезет в Postgres напрямую.

---

После сохранения этого файла у тебя появится официальная спецификация проекта — это дисциплинирует код и избавит от "архитектурного долга".
