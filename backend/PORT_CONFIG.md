# 🔌 Конфигурация портов

## Текущая конфигурация

- **Backend API:** Порт `3002` (по умолчанию `8080` в env.example)
- **Frontend:** Ожидает API на `http://localhost:3002`

**Примечание:** Порт был изменён на `3002` из-за конфликтов с другими процессами в Windows. Порт 3002 обычно свободен и не конфликтует с другими сервисами.

## Изменение порта

### Backend

1. Установите переменную окружения:
   ```bash
   $env:SERVER_PORT="8082"  # PowerShell
   # или
   set SERVER_PORT=8082     # CMD
   ```

2. Или в `.env` файле:
   ```
   SERVER_PORT=8082
   ```

### Frontend

1. Создайте `.env.local` в папке `frontend/`:
   ```
   NEXT_PUBLIC_API_BASE=http://localhost:8082
   ```

2. Или измените значение по умолчанию в файлах:
   - `frontend/lib/api.ts`
   - `frontend/app/[locale]/catalog/page.tsx`
   - `frontend/app/[locale]/product/[id]/page.tsx`
   - `frontend/components/price-chart-wrapper.tsx`

## Проверка занятости порта

### Windows
```powershell
netstat -ano | findstr :8081
```

### Остановка процесса на порту
```powershell
# Найти PID
netstat -ano | findstr :8081

# Остановить процесс (замените PID)
taskkill /F /PID <PID>
```

## Рекомендации

- **Development:** Используйте порт `8081` (избегает конфликтов с другими сервисами)
- **Production:** Используйте стандартный порт `8080` или настройте через переменные окружения

## Troubleshooting

### Ошибка: `bind: 10048` (Windows) или `address already in use` (Linux)

**Причина:** Порт уже занят другим процессом

**Решение:**
1. Найдите процесс: `netstat -ano | findstr :8081`
2. Остановите процесс или измените порт
3. Перезапустите сервер

### Ошибка: CORS при запросах с фронтенда

**Причина:** Фронтенд на другом порту/домене

**Решение:** Проверьте настройки CORS в `backend/internal/http/middleware/cors.go`

