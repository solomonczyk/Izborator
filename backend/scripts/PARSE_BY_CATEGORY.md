# Парсинг товаров по категориям

## 📋 Инструкция

Найди по одному валидному URL для каждой категории и запусти парсинг.

---

## 🎯 Категории для тестирования

### 1. Mobilni telefoni (Мобильные телефоны)
**Категория ID:** `bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb`  
**Slug:** `mobilni-telefoni`

**Как найти URL:**
1. Открой https://gigatron.rs
2. Перейди в раздел "Мобилни телефони" или используй поиск "iPhone"
3. Выбери любой товар (например, iPhone 15)
4. Скопируй URL из адресной строки
5. Пример формата: `https://gigatron.rs/mobilni-telefoni/apple-iphone-15-128gb-black-mtp03zda-573380`

**Команда для парсинга:**
```powershell
cd backend
go run cmd/worker/main.go -url "ТВОЙ_URL_ДЛЯ_ТЕЛЕФОНА" -shop "shop-001"
```

---

### 2. Laptopovi (Ноутбуки)
**Категория ID:** `cccccccc-cccc-cccc-cccc-cccccccccccc`  
**Slug:** `laptopovi`

**Как найти URL:**
1. Открой https://gigatron.rs
2. Перейди в раздел "Лаптопови" или используй поиск "laptop"
3. Выбери любой товар (например, Lenovo или HP)
4. Скопируй URL из адресной строки
5. Пример формата: `https://gigatron.rs/laptopovi/lenovo-ideapad-3-15-82h7000vra`

**Команда для парсинга:**
```powershell
cd backend
go run cmd/worker/main.go -url "ТВОЙ_URL_ДЛЯ_НОУТБУКА" -shop "shop-001"
```

---

## ✅ Проверка URL перед парсингом

Перед использованием URL убедитесь:
- ✅ Страница доступна (не 404)
- ✅ Товар есть в наличии
- ✅ Цена отображается на странице
- ✅ Есть изображение товара

---

## 🔄 Обработка сырых данных

После парсинга всех товаров, обработай их:

```powershell
cd backend
go run cmd/worker/main.go -process
```

**Ожидаемый результат:**
- ✅ Matching...
- ✅ Saving processed product...
- ✅ Processing completed processed=X

---

## 📊 Проверка результатов

1. **Проверь в БД:**
   ```sql
   SELECT name, price, brand FROM products ORDER BY created_at DESC LIMIT 5;
   ```

2. **Проверь на фронтенде:**
   - Открой http://localhost:3003/sr/catalog
   - Товары должны появиться в каталоге
   - Фильтры по категориям должны работать

---

## 🚀 Быстрый старт (интерактивный скрипт)

Используй готовый скрипт:

```powershell
.\backend\scripts\find_and_test_urls.ps1
```

Скрипт попросит ввести URL для каждой категории и автоматически запустит парсинг.

---

## 📝 Сохранение URL для будущего использования

После успешного парсинга, сохрани URL в `backend/scripts/seed_test_urls.sql` для повторного использования.

