# Izborator Backend

Backend сервис для агрегатора цен Izborator.

## 🏗 Архитектура

```
backend/
 ├─ cmd/
 │   ├─ api/               → точка входа для API-сервера
 │   └─ worker/            → точка входа для парсеров/воркеров
 └─ internal/
     ├─ config/            → загрузка env и конфигураций
     ├─ logger/            → логирование (Zerolog)
     ├─ http/              → маршруты и контроллеры
     ├─ storage/           → Postgres, Redis, Meilisearch клиенты
     ├─ products/          → доменная логика товаров
     ├─ scraper/           → логика парсеров
     ├─ matching/          → алгоритмы сопоставления товаров
     └─ pricehistory/      → сервис сохранения истории цен
```

## 🚀 Запуск

### Требования

- Go 1.21+
- PostgreSQL 14+
- Redis 6+
- Meilisearch

### Установка

1. Скопируйте `.env.example` в `.env` и настройте переменные окружения:
```bash
cp .env.example .env
```

2. Установите зависимости:
```bash
go mod download
```

3. Запустите API сервер:
```bash
go run cmd/api/main.go
```

4. Запустите воркер:
```bash
go run cmd/worker/main.go
```

## 📦 Модули

### cmd/api
HTTP API сервер для фронтенда. Обрабатывает запросы поиска, получения карточек товаров, истории цен.

### cmd/worker
Воркеры для парсинга сайтов магазинов, обработки очереди сообщений, обновления цен.

### internal/config
Загрузка конфигурации из переменных окружения.

### internal/logger
Обёртка над Zerolog для единообразного логирования во всех модулях.

### internal/storage
Клиенты для работы с:
- PostgreSQL — основная база данных
- Redis — кеш и распределённые блокировки
- Meilisearch — поисковый индекс

### internal/products
Доменная логика работы с товарами: поиск, получение карточек, цен.

### internal/scraper
Логика парсинга сайтов магазинов.

### internal/matching
Алгоритмы сопоставления товаров между магазинами (matching/deduplication).

### internal/pricehistory
Сервис для сохранения и получения истории изменения цен.

## 🔧 Разработка

### Структура проекта

Проект следует стандартной структуре Go проектов:
- `cmd/` — точки входа приложения
- `internal/` — приватные пакеты приложения
- Каждый модуль имеет свой `service.go` с основной логикой

### Добавление новых зависимостей

```bash
go get package_name
go mod tidy
```

## 📝 TODO

- [ ] Реализовать HTTP роутинг (chi, gin или стандартный mux)
- [ ] Добавить миграции базы данных (golang-migrate или goose)
- [ ] Реализовать клиент для очереди сообщений (Kafka/RabbitMQ)
- [ ] Добавить интеграцию с InfluxDB для истории цен
- [ ] Написать тесты для всех модулей
- [ ] Настроить CI/CD

