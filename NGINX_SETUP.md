# 🚀 Настройка Nginx Reverse Proxy + SSL

## Шаг 1: Генерация SSL сертификатов на сервере

```bash
ssh root@152.53.227.37
cd ~/Izborator
git pull

# Генерируем SSL сертификаты
cd nginx
chmod +x generate-ssl.sh
./generate-ssl.sh
cd ..
```

## Шаг 2: Обновление docker-compose.yml

Проверь, что в `docker-compose.yml` есть сервис `nginx` и правильные настройки.

## Шаг 3: Запуск Nginx

```bash
cd ~/Izborator

# Пересобираем и запускаем все сервисы
docker-compose build nginx
docker-compose up -d

# Проверяем статус
docker-compose ps

# Проверяем логи Nginx
docker-compose logs nginx
```

## Шаг 4: Проверка работы

```bash
# Проверка HTTP редиректа
curl -I http://152.53.227.37

# Проверка HTTPS (будет предупреждение о самоподписанном сертификате)
curl -k https://152.53.227.37/api/health

# Проверка frontend
curl -k https://152.53.227.37
```

## Шаг 5: Настройка Firewall (опционально)

Закрой старые порты, оставь только 80 и 443:

```bash
# UFW (если используется)
ufw allow 80/tcp
ufw allow 443/tcp
ufw deny 8081/tcp
ufw deny 3002/tcp

# Или iptables
iptables -A INPUT -p tcp --dport 80 -j ACCEPT
iptables -A INPUT -p tcp --dport 443 -j ACCEPT
iptables -A INPUT -p tcp --dport 8081 -j DROP
iptables -A INPUT -p tcp --dport 3002 -j DROP
```

## ⚠️ Важно

1. **Самоподписанный сертификат:** Браузер будет показывать предупреждение "Not Secure". Это нормально для тестирования.

2. **Для продакшена:** Когда будет домен, используй Let's Encrypt:
   ```bash
   apt-get install certbot python3-certbot-nginx
   certbot --nginx -d izborator.rs
   ```

3. **Проверка логов:** Если что-то не работает:
   ```bash
   docker-compose logs nginx
   docker-compose logs backend
   docker-compose logs frontend
   ```

## 🎯 Результат

После настройки:
- ✅ `http://152.53.227.37` → автоматический редирект на HTTPS
- ✅ `https://152.53.227.37` → доступ к приложению
- ✅ `https://152.53.227.37/api/health` → API работает
- ✅ Безопасное соединение (HTTPS)

